# Agent セッションライフサイクルの理想形（不変条件）

作成日: 2026-07-07
更新日: 2026-07-19

milestone 84「Agentチャット安定化」のドキュメント群:

- [agent-chat-instability-audit.md](agent-chat-instability-audit.md) — 問題点インベントリ（全 66 件、要求リスト）
- [agent-chat-ideal-vocabulary.md](agent-chat-ideal-vocabulary.md) — 正規化語彙・データ構造の理想形
- **agent-chat-ideal-lifecycle.md（本書）** — ライフサイクルの理想形（不変条件）
- [agent-chat-ideal-presentation.md](agent-chat-ideal-presentation.md) — UI 表示の理想形
- [d3-durable-event-store-design.md](d3-durable-event-store-design.md) — local atomic event store の物理設計 gate
- [close-quit-decision-table.md](close-quit-decision-table.md) — view close / Session lifecycle / configuration transition / application quit の surface 別正本

本書は「セッション・turn・permission・queue が、どの経路を通っても何を保証するか」を不変条件（invariant）として定義する。監査で確定した lossy-lifecycle / divergent 問題群の解消先。語彙は vocabulary 文書の型を前提とする。

## 設計原則

- **L-P1 (durable-first)**: UI が表示する全状態は durable event → read model から復元可能でなければならない。transient event（Tauri emit）は同じ結果を早く見せるための加速手段であり、正しさに関与しない。
- **L-P2 (終端の明示)**: turn・permission・tool call・queue item は、どの経路（正常・中断・切断・クラッシュ）でも必ず終端状態に到達し、その終端が durable に記録される。「進行中のまま忘れられる」状態を作らない。
- **L-P3 (失敗の着地)**: 内部の失敗（永続化失敗・emit 失敗・backend 応答エラー）は握りつぶさない。canonical mutationの失敗はprovider I/O前ならoperation rejection、I/O後ならdurable reconciliation obligationへ着地させ、Notice / Error / logだけで成功扱いしてはならない。commit後のemit / telemetryだけがbest-effortであり、失敗時は構造化logとsnapshot再取得へ着地できる。
- **L-P4 (backend 差の吸収)**: 停止・生存確認・頑健性の保証水準は backend に依らず同一とし、差はランタイム層が吸収する。
- **L-P5 (configuration authority)**: provider/model / Agent mode / Reasoning effort のmutation authorityは Rust-owned `AgentSessionConfiguration` aggregate、durable authorityはcanonical `SessionConfigurationSelected / SessionConfigurationActivated` eventとする。Goal mutationの正本は独立したRust-owned `AgentGoalState`とcanonical Goal eventである。`AgentSessionConfigurationProjection`と`SessionGoalProjection`はbackend queryがpinned snapshot上のcommitted event / projection dataとcapability / policy dataから直接組み立てるread modelであり、mutation authorityにしない。configuration操作の可否は前者の`available_actions`、Goal操作の可否は後者の`available_actions`としてbackend queryが評価する。frontendの入力値、providerの未確認値、各turnの送信値を正本にしない。
- **L-P6 (atomic event visibility)**: 「同じlocal atomic batch」はvocabulary §9.5のdomain port `LocalEventTransactionRepository.commit_batch`を通して背後の`LocalEventTransactionStore`が確定するtransactionを指す。launch / Session / Goal / workflow / queueの全participant eventはcommit前に不可視、commit後に一括可視とし、独立logへの逐次appendをatomicと呼ばない。queryはrequired projection sourceのcommon readable watermark以下へpinした1つのsnapshot leaseを全sourceの`read_at`へ渡し、途中失効・`ProjectionBehind`時は部分結果を捨ててquery全体を再取得する。watchは`open_watch`がsnapshot/replay判定・common-watermark lease取得・barrier後subscription/receiver登録を同じcommit境界で行い、`receive`が各notice commitへ厳密にpinしたlive fenceを返す。同じportがhandle単位のbootstrap/update完了、bounded buffer、受信、disconnect時のcloseを所有し、usecase-owned watch serviceがfenceからtyped client frameを構築する。

## 不変条件

### I1: turn 終端保証

実際にturnを終了させる経路 — 正常完了、interrupt、Fatal、normal Session close、open-session archive、graceful application shutdown — では、`TurnResult`のdurable記録とstreaming partsのFinal化（`FinalPartsRecorded`）を実行する。

- ギャップ: RT-1（close/切替/終了時に finalize も flush もされない）
- normal Session closeとopen-session archiveはstable close operation IDを発行する。runtimeがある場合、初期lifecycle closureはactive turnならFinal parts、`TurnResult::Interrupted { reason: SessionClosed }` terminal、permission settlement、`QueuePaused`、Closed / Archived projectionと、起動時から保存したstable runtime instance / epochの`OwnedShutdownScopeRef`＋exact effectをboth Someにした`SessionClose(Pending)`予約を1manifestで確定する。Idleならsynthetic terminalを追加せず`QueuePaused`、Closed / Archived projectionと同じPending予約を確定する。その後`EffectReserved`へCASしてからscope配下のruntime / child groupを扱うshutdown portを1回呼び、観測結果を保存する別closureで初めてcompact `Terminal`へ進める。runtimeが無ければ、初期lifecycle closureがscope / effect both Noneのcompact `Terminal(Succeeded)`をprojection等と直接確定し、pending inventory / claim /空のexternal effectを作らない。runtime close結果不明はClosed / Archived、queue pause、既存terminalを維持した`ReconciliationRequired`とし、blind retry / Session reopenを行わない。graceful application shutdownはI17の全target prepareとglobal activationが完了した後だけcurrent coordinatorが各targetを同じclosureへ進める。backendの終了eventを捨てない。
- chat / panel、workflow node tab、workspace view、windowのview closeはturn終了経路ではない。active turn、permission、queue、runtimeを変更しない。
- backend switchはIdleかつpending permission / recovery / external-effect obligationが無い場合だけ受理するD1 configuration transitionである。old runtimeがある場合、初期D1 closureはdesired backend、old effective configuration guard、`QueuePaused`、既存item、不整合itemのNeedsResolution化と、stable scope＋effectをboth Someにした`SessionClose(Pending)`予約を同じdurable intentとして確定する。`EffectReserved`後にscope単位で1回closeし、観測結果を保存する別closureでTerminal化した後だけnew configurationをeffectiveにする。old runtimeが無い場合は初期D1 closureがscope / effect both Noneのcompact `Terminal(Succeeded)`とnew selected / effective configurationを直接確定し、pending recoveryを残さない。runtime close結果不明はold effective configurationとqueue pauseを維持した`ReconciliationRequired`とし、new backendを自動起動しない。条件を満たさなければtyped Busy / PendingOperationを返し、flush、SessionClosed terminal、queue mutation、runtime close、backend selection変更のいずれも行わない。旧I1 / #1405のactive backend switch契約は#1499により本規則へsupersedeする。
- active / Idle normal Session close、active / Idle open-session archive、closed Session archive、Idle backend switchはrequest ingressのsame-boot monotonic T0から10秒をcommand全体のabsolute deadlineとする。初期closureがBeforeCommitなら元stateを保ってeffect 0、OutcomeUnknownならsame operationをresolveする。初期closure後のruntime closeが未応答ならClosed / Archived、paused queue、activeだけのSessionClosed terminal、switchではold effective backendを保った同じidentityのReconciliationRequiredを10秒以内に返す。closed archiveはqueueを変更しない。10秒後のlate completionはoperation revision / claim / runtime generationで拒否し、terminal、runtime close、session reopen、new backend startを重複させない。
- view closeを除くClose / ArchiveOpen / ArchiveClosed / SwitchBackendはTauri専用`request_session_lifecycle`へ正規化する。caller request IDは1..=128 ASCII bytesの`[A-Za-z0-9._:-]`で、same principal / same key / same exact commandは保存済みreceipt / state / outcomeをexact replayし、same key / different commandは`PayloadConflict(SessionLifecycle)`としてbinding、operation、queue、terminal、runtime effectを0件にする。別key / same principal / same unresolved session / same normalized actionは既存operationへjoinしてbindingだけを追加し、first accepted revision guard、receipt、action、deadlineを変更しない。SwitchBackendはbackend IDまでをaction identityに含める。別actionは`PendingOperation`で新binding / operation / effect 0件とする。same-key raceはcaller-key winner一件、different-key raceはsession single-flight winner一件へ収束させる。session authorizationはlookupより先に検査し、unauthorized commandとcross-principal operation queryは存在を秘匿したNotFoundとする。
- lifecycle acceptanceはbackend発行opaque `SessionLifecycleOperationId`、first expected revisionを含むimmutable receipt、必要なqueue / terminal / configuration participant、normal `SessionClose` kind 7 obligationまたはdirect terminal result、10秒deadlineを一つのclosureへ確定してからeffectを開始する。writer開始前またはrollback確認済みfailureだけがRejectedBeforeAcceptance、writer結果不明だけが同operation IDのOutcomeUnknownである。Accepted後はreceiptを維持したInProgress / ReconciliationRequired / Completedだけへ進み、10秒で未確定ならReconciliationRequiredとする。restartとresponse喪失後の`get_session_lifecycle_operation`はoperation direct authorityと保存済みkind 7 resultからexact replayし、current Sessionからoutcomeを再構築しない。BackendSelectedのruntime_startedは次sendまでfalse、closed archiveはqueueを変更せず保存済みqueue_pausedを返す。view closeはrequest ID、backend operation、obligationを作らない。
- lifecycle exact bindingはapp-data generationのowner-only共通keyと`session-lifecycle-exact-request-binding/v1` domainを使う。key bytes `00..1f`、principal `principal_1`、generation `app_1`、request `lifecycle_req_1`、operation `lifecycle_op_1`、session `session_1`、revision `1`、action `close`、backend `none`のKATはinner 38 bytes、full preimage 149 bytes、HMAC-SHA256 `b623c791f1a3f40579ba9713507ab507bdc844dee12d95e4408d673b17eb2217`である。same-key replay / conflict、different-key join、restart importの全経路が同じcodecを通る。

### I2: クラッシュ回収

アプリ起動時にpending-only obligation inventoryからdangling executionを発見し、`TurnStarted`があり終端eventが無いturnを`Interrupted { reason: Crash }`でfinalizeする。個別Sessionの初回load、全Session/event走査、通常session listをrecovery discoveryに使わない。permissionは状態で分け、未送信の`Pending`だけを`Cancelled(effective=false)`へ畳む。write-ahead済み`Responding`/`Resolving`はresponse/resolution intentを保持し、provider cancel/tool-start/decision/readbackを相関して確定不能ならpermission reconciliationへ送る。`ReconciliationRequired`をCrash理由で上書きしない。tool call は `Interrupted` に畳む。

- ギャップ: RT-2（回収経路が無く、スピナー・確認待ちが永久残留）
- #1499は導入後のdurable operation / obligationをstartup inventoryから回収する。導入前のlegacy active turnについて、terminal identity、provider effectの開始・結果、runtime guardを保存済みdataから一意に証明できない場合は通常terminalを捏造せずReconciliationRequiredへ保守変換する。legacy dangling turnの完全なterminal化とfork recovery stateの整理は[#1406](https://github.com/siro33950/releash/issues/1406)が所有する。
- Phase 0 bridge初回upgradeは利用者の明示migrationなしでautomatic bootstrapを行う。startupはexclusive app-data writer lockを取得し、mutation / provider / workflow / recovery / close / backend switch / normal shutdown admissionを閉じたままauthority pointerを読む。pointer未作成またはLegacyならimmutable source inventoryを固定する。read batchは最大200 source logical recordsまたはdecoded source 16 MiBの先到達側で閉じ、staged payloadをread上限へ合算しない。staged candidateは別physical K tier（最大K4 17 MiB）へ収め、normal lane 64 MiBの同時収容上限を検査する。一つのsourceは必要ならdeterministic substepへ分割してcursor未前進のmicrocommitで再開可能にし、全substep / source parity finalize後だけsource cursorとimported countsを進める。partial substepはpublic projection / parity authorityへ出さない。stagingはnormal authorityではなくlegacy bytesを変更しない。crash / response loss後はstate / source inventory / completed substep / batch / staged rootをhash検証して同じsource / substepから再開し、新旧sourceを混ぜない。
- bootstrap中もTauri `get_phase0_bootstrap` / WebSocket `GetPhase0Bootstrap`は同じ`Option<Phase0BootstrapProjection>`を返す。未開始source inspection、batch import、parity verification、pointer activation、failureを`InspectingSource / Importing / Verifying / Activating / Failed`へ一意に写し、全phaseをread-only、Failedだけをsafe failure Someにする。pointer CAS後もPhase0 pointer / root / parity検証、reachable manifest replay、pending inventory validation、normal read / mutation admission openまでは同bootstrap IDのActivatingを維持し、その完了後だけNoneとする。storage / decode / pointer OutcomeUnknownをNoneへ潰さず、frontendはphase / count / failureを合成しない。
- bootstrap中のquitだけは無効化せずbootstrap-safe bounded exitへ送る。normal shutdown plan / target / obligationを作らず、backend発行opaque `ApplicationQuitOperationId`、caller request binding、bootstrap flight locator、`Settling` flight recordを一つの受理closureで確定し、`ApplicationQuitProjection::Bootstrap`を返す。special flightは最初に受理したtyped ingressのcanonical `ShutdownExitIntent`を固定する。同じrequest ID / different intentは`ApplicationQuit { request_id }` identityを持つtyped `PayloadConflict` application error、別request IDの後続quitはfirst intentの同flightへjoinし、そのcaller keyから同じbackend operation IDへのexact bindingを保存する。quit T0からcurrent batchをCommittedまたはrollback済みBeforeCommitへcheckpointし、pointer writer開始後は同じattemptへ13秒までjoinするが、明示agent / workflow shutdown commandは作らない。OutcomeUnknownをLegacy / Phase0へ推測せず15秒に同intentのone-shot permitでprocess exitし、fresh bootがpointer / stateを先に解決してsame source cursorから再開し、旧coordinator bootのflightを`Exited`へ確定する。operation queryはnormal planとbootstrap flightをlocatorで一意に分岐する。
- quit binding codecの固定KATはkey bytes `00..1f`、principal `principal_1`、generation `app_1`、request `quit_req_1`、backend operation `quit_op_1`、mode `exit`、exit code `0`からcanonical preimage 112 bytes、HMAC-SHA256 `6a34bd12ce2691c1912e31d4e0f797cd51e28a67fdf5dc03714f18782e49dfda`を得る。lifecycle acceptance testはpreimage bytesとdigestを固定し、same caller replay / different intent conflict /別caller joinの全経路が同じcodecを通ることを検証する。
- normalizationはterminal / permission exact bytes / unknown raw bytesを保持する。effect開始 / 結果を証明できないactive turnはReconciliationRequired、runtime / configuration guardを証明できないqueueはPaused＋ReconciliationRequired、exact private permission payloadを保持できないidentityはFailedまたは最小scope quarantineへ写し、自動provider start / resume / interrupt / response / queue drainを行わない。source inventory全体を固定不能ならglobal Failed、局所破損はSession / Workflow / orphan scopeだけをquarantineして他scopeを継続する。quarantineはsource inventory entryとbyte一致するexact legacy raw bytesをowner-private random-ID immutable blobへ1 MiB chunk / step 16 MiBでcrash-resumableにcopyし、final byte length / SHA-256 / owner accessを検証・syncした後だけrecordをcommitする。raw bytes / blob ref / path / digestはpublic view / logへ出さず、legacy sourceのparity検証、Phase 0 activation、F3 one-shot importに必要なretentionが全て完了するまで保持し、Phase 0ではreset / purgeをGC triggerとして扱わない。
- 全batch後にcounts、scope membership、terminal uniqueness、closed normalization rulesを同じlegacy bytesへ適用したexpected normalized message / permission / queue projection、unknown raw bytes、ordered quarantine raw bytes hash、COW root / direct parityを検証する。旧UI文字列との一致ではなく、確定済みtranscript / terminal / owner relationを維持しつつ、証明不能active / queue / permissionをReconciliationRequired / Paused＋ReconciliationRequired / Failedまたはquarantineへ保守変換したexpected projectionとの一致を要求する。required sync後にauthority pointerをLegacyからexact Phase0 rootへCASする。このpointerだけがauthority切替commit pointで、CAS前crashはlegacy、CAS後response lossはpointer / rootを再検証してPhase0へ収束する。切替前はlegacy read-only、切替後はPhase0 reader / writerだけとし、dual write、per-record fallback、query merge、legacy write-back / rollbackを禁止する。physical identity共通builderはclosed ASCII prefix、u16 big-endian component count、各componentのu32 big-endian length＋raw bytesを使う。1..=16 component、各1..=1024 bytes、pre-hash canonical input最大16 KiB、生成後logical key 1..=1024 bytes、unsigned numeric=u64 big-endian 8 bytes、enum=closed ASCII tag、Unicode=raw UTF-8、schema v1 signed numeric禁止を検査し、超過 / empty / unknown prefix / signed numericをtruncate、decimal ASCII、別hashへfallbackしない。
- #1499の完了gateは前項のPhase 0 bootstrap / closure fault injectionとTauri / WebSocket parityまでとする。Phase 0→F3 one-shot cutoverの契約・verificationはD3で確定するが、cutover runtimeの実装・実行は#1499へ含めずF3 #1385で行う。
- 要点: startupは3-tree root envelopeのcanonical primaryから、`TurnExecution / QueueExecution / PermissionDelivery / ProviderEstablish / TerminalCommit / BackendRecovery / SessionClose / WorkflowShutdown / RecoveryPublication`のpending obligationを各1件だけ、最大200件かつtransport wrapper込みencoded 4 MiBの先到達側でpage列挙して未完了回復を発見する。decoded 4 MiB上限を使うのはexact shutdown snapshot drill-downだけである。owner / association secondaryを重複countせず、全Session / partition scanへfallbackしない。Pending / TerminalをID lookupする別のimmutable obligation-state-by-ID rootを同じ`Phase0ReadSnapshotRef`へpinし、discovery entryとcanonical latest stateを別revisionから混ぜない。結果はdirect obligation、obligation-state root、pending中だけ存在するprimary / secondary / root envelopeを同じmanifestでdurableに書き戻し、transaction-inventory commit前にlogical rootを前進させない。reloadの度に再判定しない。通常Sessionはsession list、workflow-owned Sessionはowning run / node、closed Sessionはclosed historyに回復状態を投影し、closed Sessionを自動reopen / provider resumeしない。
- backend recovery開始時にactiveな旧`TurnStarted`があれば、最新durable partsのFinal化と`Interrupted(Crash)`、`BackendSessionRecoveryStarted`を同じlocal atomic batchで確定してからretryする。retry turnは新idにし、`retry_of_turn_id / recovery_id`で相関してhuman inputを複製しない。
- recovery claimはpending lifecycleと別recordでowner boot id、claim generation、token、30秒leaseを持ち、claim中crashはlease expiry後に同じobligationを世代更新してreclaimする。obligationのdomain ownerは`Session / WorkflowExecution / OrphanRuntime { runtime_instance_id, runtime_epoch, discovery_generation }`のclosed enumであり、claim worker ownerとは分ける。application quitのtarget obligationもSession / Workflowの実ownerを変えず、immutableな`ApplicationShutdownAssociation { plan_id, epoch, target_key }`を別fieldに持つ。normal close / recoveryはassociationなし、quit targetはassociation必須とし、completionとplan queryはassociationで旧planをfenceする。runtimeを元ownerからdetachする場合は、detachmentとOrphanRuntime recovery obligationの作成 / handoff、`UnownedRuntime` inventory insertを同じtransactionで確定し、memory-only orphanをstartup recovery対象にしない。obligation terminal resultは`Succeeded / CancelledBeforeEffect / Superseded / FailedTerminal`のclosed 4値であり、dependencyを満たすのは`Succeeded`だけである。dependencyはcreation後immutableなfixed-kind DAG / max depth 1とし、`TurnExecution / QueueExecution`は同owner・同operationのdistinct `ProviderEstablish`を0..=3件、`RecoveryPublication`は同recovery identityの`BackendRecovery`をexactly 1件、他kindは0件だけ許す。creation / claimの両方でkind・identity・件数を最大3 direct lookupで検証し、任意graph traversalを行わない。manual actionも根拠に応じて4値のいずれかを確定し、第5のgeneric resultを作らない。`BackendRecovery`はprovider / Session recoveryだけを所有し、payloadへprovider create / resume / readback用の必須effectと独立`RecoveryPublication` obligation IDを持つ。message ID / payload ref / publication markerのauthorityはRecoveryPublicationへ一本化する。RecoveryPublicationはexternal effectではなくlocal publication obligationで、BackendRecovery completion closureがBackendRecovery `Terminal(Succeeded)`とRecoveryPublication `Pending`を同時確定する。publication workerはlocal claimを取るが`EffectReserved`を通らず、message公開、publication marker、RecoveryPublication `Terminal(Succeeded)`を次のpublication closureで一度だけ確定する。
- canonical stateは`ObligationStateRecordV1::Pending(PendingObligationRecordV1) | Terminal(ObligationResultRecordV1)`のclosed enumで、pending inventoryにはPendingだけを置く。Pendingのbounded `reconciliation_reason`はReconciliationRequired専用で他stateはNone、Failed reasonはstate variantだけをauthorityにする。Terminal transitionはpending entry / claimを削除し、payload / dependency / claim generation / turn・operation ID / permission private ref / authoritative proof refを持たないcompact resultへstate-indexを進める。terminal recordはowner / shutdown association / kind / semantic correlation / 4値result / safe observation / bounded safe failure / UTC completed_at / revisionだけを保持し、proofに由来するsafe observation内のdigestだけを残す。他のlive refが無いpermission / proof private blobはこのcommit後にGCでき、Terminalからprovider I/Oを再構築しない。
- Pending / Terminalはdomain-separated kind / owner / association / kind-specific stable public IDsの`semantic_correlation_sha256`を共通保持する。TurnExecution / QueueExecution / ProviderEstablishは同じcaller指定`SendOperationId`をcorrelationへ含める。send acceptance由来の3 kindはgeneric `operation_id=Some`を必須とし、Turn / QueueがProviderEstablishを参照するcreation / importではparent / dependencyのoperation IDをbyte一致検証する。Pendingはordered dependency IDsとsame-length max3のdependency binding hashも保持し、creation / importでkind / owner / operation・recovery relationとdependency current correlationを固定する。claimは最大3 direct Terminal lookupでbinding hash一致＋Succeededを再検証する。Terminalは自身のcorrelation hashを残すがdependency IDs / bindings / payloadを残さないため、compact済みProviderEstablishを別operationへ流用できない。
- compact TerminalのFailedTerminalはsafe failure Some、Succeeded / CancelledBeforeEffect / SupersededはNoneを必須とする。public viewはpayloadless Pending stateとseparate safe failureへ写し、Pending Failedはstate内reason、ReconciliationRequiredは専用`reconciliation_reason`がある場合だけSome、他PendingはNoneとする。frontendで二つのfailure authorityをmergeしない。
- 全`ExternalEffectIntent.external_correlation_key`はUTF-8 1..=512 bytesを必須とする。`IdempotentRetry | IdempotentRetryAndReadback`はstable idempotency key 1..=512 bytesを必須、`AuthoritativeReadback | IdempotentRetryAndReadback`はprovider-native stable correlationを一意に読めるadapterだけを許し、`None`はidempotency keyなし・automatic retry / readback禁止とする。common builder / import / claimで同じvalidatorを通し、invalid pairはeffect前にreject、既存不正stateはquarantineする。
- kind / payload / effect / owner / optional correlationはvocabularyのclosed 9-kind matrixをbuilder、replay、import、claim、action resolverで共通検証する。Turn / QueueはSession owner、association None、turn / send operation Some、Provider-owned TurnStart / QueueTurnStart、Session dispatch fence runtime epoch Some、5 pending lifecycle、ProviderEstablish dependency 0..=3だけを許す。TerminalCommitはSession owner、association None、turn Some / operation None、Runtime-owned Stop専用ProviderInterrupt、EffectReserved / ReconciliationRequired / Failedだけ、PermissionDeliveryはSession owner、association None、turn / operation None、Provider-owned PermissionResponse、5 lifecycleである。ProviderEstablishはSession owner、association None、turn None / operation Some、ProviderCreate / Resume、5 lifecycle、BackendRecoveryはSessionまたはOrphanRuntime owner、両correlation None、対応ProviderCreate / Resume、5 lifecycleである。SessionCloseはSession owner、両correlation Noneで、application quitだけassociation Some、scope / effect both SomeならRuntimeClose＋Session fenceで5 lifecycle、both NoneならPending禁止のdirect Terminal(Succeeded)である。WorkflowShutdownはWorkflow owner、両correlation None、Workflow effect / fence、5 lifecycleである。RecoveryPublicationはsource BackendRecoveryと同じSession / Orphan owner、association / correlation / external effect / dispatch fence / provider proofなし、BackendRecovery dependency exactly 1、Pending / ReconciliationRequired / Failedだけを許しEffectReservedを禁止する。wrong-kind / wrong-owner / unexpected Some・Noneはeffect 0件でrejectまたはquarantineし、推測補完しない。
- authoritative result / no-effect proofはprovider / runtime / workflow adaptorが認証済みresponseまたはcanonical eventから作るdurable `AuthoritativeEffectProofRefV1`だけをauthorityにする。effect ID、external correlation SHA-256、safe observation SHA-256をcurrent intent / observationとbyte一致させ、private blobは1 byte以上1 MiB以下、schema / size / content hash / owner accessを検証する。proof kindはEffectStarted / Succeeded / Terminal / AuthoritativeNotFound / ConfirmedNoEffect / Ambiguousの6値だけで、client supplied observationを受け付けない。proof ref、Pending revision、safe observationを同closureで確定し、public viewにはsafe observation / digestだけを出す。private blobは全参照消滅後だけGCし、Completed action replayはcanonical safe resultを別途保持する。
- external I/Oは唯一のRust-owned `EffectDispatchGate(effect_id)`を通す。dispatcherはadapter invocation直前にexact Pending(EffectReserved)、terminal Absence、record revision、claim generation / token / boot / lease、effect ID / intent hash、owner / association、dispatch fenceのowner revision / command・runtime・executor generation、scope-bearing effectのunresolved fence、external recovery action attempt generationを再検査する。shutdownはActivated lineage / page hash / ordinal / cutoffも検査する。gate内handoffはnon-async / non-blockingで、filesystem / socket / process I/O、provider response、Session / runtime-event lockその他のlock待ちを禁止する。immutable commandをcancellation-shielded owned driverへ移し、I/O開始権と一体のstarted handleをeffect registryへtry-registerするだけとする。登録不能ならdriverをI/O前に破棄してeffect 0件、登録成功後のresult waitはgate解放後にabsolute deadlineの残予算だけで行う。terminal / resolution / reclaim / invalidationも同じgate＋registryでfirst-winnerを決め、未検査queueへの遅延first dispatchとhandoff hangを許さないため、Stopの10秒budgetを同期handoffが消費しない。全effect pathでguard不一致はeffect 0件とする。
- Recovering/ReconciliationRequired中のforkはblockする。stable forkはcommitted historical stateのallowlistから構築し、親固有のpending recovery message、recovery id、未完了intent、delivery obligationを継承しない。

### I3: streaming flush 保証

streaming 中の本文・tool 出力は一定間隔（現行 1s の定期 flush を保証として明文化）で durable 化され、損失窓は最大 flush 間隔に限定される。turn 終端では必ず Final 化する。

- ギャップ: RT-2（クラッシュ時に直前約 1 秒が消える — これは許容損失として明文化）、FE-3（hydration が flush 済みデータを取りこぼす — presentation 側 P1 で解消）
- 要点: 「1 秒」は保証値として定数化し、テストで検証する。

### I4: queue の永続と一貫性

pending queue は durable であり、アプリ再起動・session close・backend 切替を跨いで生存する。queue item の終端は「実行開始 / ユーザー取り消し / 起動失敗」のいずれかで、全て記録・可視化される。

- ギャップ: OB-3 / RT-3（メモリのみで無言消滅）、OB-4（取り消しても human message が復活）、OB-6 / RT-7（起動失敗で無言停止）
- 要点:
  - queue eventをSession event logの正本とし、message/queue storeは再構築可能なprojectionにする。human message記録と`QueueItemEnqueued { item_id, message_id, input_ref, snapshot_hash }`、取消時のmessage cancelled markerと`QueueItemCancelled`は、それぞれ同じlocal atomic batchでappendする。片方だけ永続化される窓を作らない。
  - queue item は enqueue 時点の effective configuration revision と resolved mode / reasoning effort の小さな snapshot、`goal_id + goal_revision` を保持する。後続の Session 設定変更で既存 item を暗黙更新せず、変更する場合は revision conflict を検査する明示的 rebase command を使う。
  - drain 時は provider / model capability と Goal の active 性を最新contextで再検証する。Goal が completed / cleared / replaced、またはprovider/model/mode/effort/effects/residual protections/managed policyのsemantic結果がenqueue snapshot hashと変わった場合は、古い item を暗黙更新せず`QueueItemResolutionRequired`をappendして`NeedsResolution`とする（回復不能時だけ`Failed`）。明示`QueueItemRebased`だけがsnapshotを更新する。`checked_at/evaluated_at`等の観測時刻はcanonical semantic hashから除外し、意味が同一ならTurnStartedに最新revalidation evidenceだけを添える。
  - rebaseは`item_id + expected item revision + expected snapshot hash + resolution_id`をCASし、new snapshot/hashを持つ`QueueItemRebased`をappendして初めて`Queued`へ戻す。projectionだけを書き換えない。Stop受理時は`TurnInterruptRequested + QueuePaused`をprovider I/O前の同じlocal atomic batchでappendし、終端前にcrashしても自動drainを止める（既にpausedならidempotent）。resumeはユーザー操作によるCAS付き`QueueResumed`だけで行う。
  - adapter は per-turn override があれば snapshot のcanonical semantic値を turn start payload だけへ適用し、Session の selected / effective configuration は変更しない。ただしsnapshot modeがBypassのqueue itemはrevision差の有無にかかわらず毎executionでfresh challengeを要求し、最新再評価で得たeffects/residual protectionsをchallengeへ含める。`prepare_queue_execution`がexecution id/snapshot hashをreserveし、`QueueExecutionPrepared + BypassChallengeIssued`を同じlocal atomic batchでappendして`AwaitingBypassConfirmation`へ移す（non-Bypassはprepare不要）。確認後だけconsume＋start intentへ進み、失効/変更時は新reservationを要求する。provider が session-scoped 更新しか持たず item snapshot が current effective と異なる場合は、一時適用・復帰を行わず `NeedsResolution { reason, resolution_id, actions: [Rebase, Cancel] }` にして明示 rebase を要求する。
  - 取り消し時は永続化済み human message を「cancelled」としてマークし（削除しない。マークは message read model へ durable に記録し reload 後も保たれる）、復元コンテキストからも除外する（OB-4）。
  - non-Bypass drain、またはBypass confirmation後のstartは`QueueExecutionStartIntent { item_id, execution_id, snapshot_hash, bypass_confirmation }`を受け、`QueueExecutionRequested`をappendして`Starting`にする。Bypassはchallenge id / nonce・期限・guard・caller scopeを検証し、`BypassChallengeConsumed`を同じpre-I/O batchに含める。独立configuration activation済みproviderではさらに`QueueItemStarted + TurnStarted { queue_item_id, queue_execution_id }`を同じbatchでappendし、その後だけ送る。turn/start payloadでper-turn overrideを適用するproviderは`TurnStartRequested`を同じpre-I/O batchへ含め、ack後に`QueueItemStarted + TurnStarted`だけをatomic commitする。queue snapshotで`SessionConfigurationActivated`をappendせずSession effectiveを変更しない。restart時、TurnStartRequestedが無いStartingは`Failed(InterruptedBeforeStart)`へ決定的に回収し、未完TurnStartRequestedがある場合はprovider送信結果不明としてTurnStartReconciliationへ移す。どちらも暗黙再送しない。
  - queue item status は `Queued / AwaitingBypassConfirmation / Starting / Started / Failed / Cancelled / NeedsResolution` を型で持つ。起動失敗した item は `Failed` として残し、ユーザー操作（再試行・取り消し）を待つ。無言のまま次の送信で暗黙復活させない（RT-7 の解消）。
  - Failedの再試行は`item_id + expected item revision`をCASし、新revisionを持つ`QueueItemRequeued`をappendして同じimmutable input/snapshotを`Queued`へ戻す。以前のexecution/challengeを再利用しない。Bypassなら必ず新しい`prepare_queue_execution`とchallengeを通す。semantic snapshotを変更する場合はretryでなく明示rebaseを使う。
  - queue pauseは`queue_paused_at + queue stream head/revision + applied_through_global_commit_seq`を持つ小さなprojectionとして、`QueuePaused / QueueResumed`と同じcommitから更新する。steady-state queryはlegacy event、tail、batch directoryをread/hashせず、`ProjectionBehind`時に通常pathで全履歴fallbackしない。legacy import時の一度きりのidempotent seedだけを例外とする（F10 #1497）。
  - normal Session close / open-session archive、backend switch、active / Idleを問わないgraceful application shutdownはqueue itemを削除せず`QueuePaused`を同じdurable manifestへ含める。pauseまたは既存pauseを維持し、restore / restart後も明示`QueueResumed`までdrainしない。
  - queueが次itemを自動開始できる終端理由は、providerが確定した通常の`TurnResult::Completed`だけである。その時点ですでに`paused=false`で、configuration / Goal / policy再検証にも成功した場合だけ次itemを1件開始する。Stop、normal Session close、open-session archive、backend switch、graceful quit、Fatal、Failed、Timeout、Crash、ProtocolIncompatible、recovery / reconciliationはqueueをpauseまたは既存pauseを維持し、自動drainしない。close / quitでqueue item自体は削除しない。
  - backend switchは既存itemを保持してpauseし、旧backend / configuration snapshotとnew effective configurationが不一致のitemを`NeedsResolution`へ移す。暗黙rebaseしない。
  - view closeはqueueを変更せず、active workflowによるdrainも止めない。hard kill後はstartup recoveryがRecoveryHoldを設定し、reconcile完了と明示resumeまで自動drainしない。

### I5: interrupt 保証

Stop要求を無言no-opにしない。request admission時にprocess-wide unresolved-admission occupancyとforce-finalizer schedule slotを先取りし、acceptance commit待ち中もoccupancyを保持する。target turn、runtime epoch、interrupt intent、queue pause、absolute deadline / epoch fence、permit refを持つ`TerminalCommit(EffectReserved)` obligationとそのclaimを同じStopAcceptance manifestでprovider I/O前に確定できた場合だけAcceptedとする。StopではAccepted後の別reserve commitを挟まない。distinct未完了targetは最大32件で、same-target duplicateは同じoccupancy / Stop identity / resultへjoinする。33件目またはdeadline slotを確保できないStopはprovider interrupt前にtyped `StopCapacityExceeded`を返し、受理済みとは表示しない。Accepted後は確定済みclaimに基づいてbackend interruptを送り、送出が不可能・無応答でもstorage正常なら10秒以内にturnを強制終端する。

- ギャップ: OB-1 / SD-2（Codex は turn_id 未取得窓で無言 no-op、フォールバック無し。frontend も再押下を握りつぶす）
- 要点:
  - caller指定`request_id`は1..=128 ASCII bytesの`[A-Za-z0-9._:-]`だけを受理し、不正値は`InvalidRequest`としてStop operation / state / provider effectを0件にする。exact payloadは`session_id / target_turn_id / expected_session_revision`の3 fieldであり、同じprincipal / request ID / exact payloadだけが同じStop decisionをreplayする。同じrequest IDで3 fieldの一つでも異なる場合はexact 19種の`SessionOperationFailureKind`や`StopResult`へ追加せず、typed `PayloadConflict` application errorとしてeffect 0件で返す。別request ID / same unresolved session・turnは既存Stopへjoinし、後続expected revisionはcaller bindingへ保存するが初回Accepted guardを変更しない。backend発行opaque `StopOperationId`は別identityである。
  - Stop binding codecの固定KATはkey bytes `00..1f`、principal `principal_1`、generation `app_1`、request `stop_req_1`、backend operation `stop_op_1`、session `session_1`、turn `turn_1`、expected revision `1`からcanonical preimage 129 bytes、HMAC-SHA256 `9aea744029168a755e77bf7fa763f84df36b2167f7b1bc7fc727e75a26590d3c`を得る。lifecycle acceptance testはpreimage bytesとdigestを固定し、same caller replay / payload conflict /別caller joinの全経路が同じcodecを通ることを検証する。
  - turn_id 未取得窓の Stop は「turn_id 取得後に interrupt を送る」予約として保持する。
  - `StartingTurn`（TurnStartRequested後/ack前）のStopも常に受理し、起点や現在のitem有無に関係なく`TurnInterruptRequested + QueuePaused`（既にpausedならidempotent）をatomic appendして`TurnStartState::InterruptRequested`へ移す。late ackを通常のTurnStarted commitへ流さず、取得したprovider turnをinterruptしてInterruptedでfinalizeするか、ack/interrupt結果不明なら同じrequest idのTurnStart reconciliationへ畳む。
  - interrupt 後の queue は自動 drain しない（OB-5）。Stop受理を`TurnInterruptRequested + QueuePaused + TerminalCommit(EffectReserved) + claim`のpre-I/O atomic batchで確定してからbackend interruptを送り、再開はユーザー操作によるCAS付き`QueueResumed`だけにする（presentation 文書 §queue）。
  - request ingressでoccupancy / deadline slotを確保したsame-boot `tokio::time::Instant`を暫定T0とし、absolute public deadline `T0 + 10s`を固定する。StopAcceptance commit成功時だけT0に対応するUTC `accepted_at`をpublic Accepted factとして確定し、durability確認UTC時刻は別の`acceptance_committed_at`へ監査記録する。same bootのforce / 9.5秒 / 10秒判定はmonotonic Instantだけをauthorityとし、wall clockやpersisted timestampで延長しない。commit失敗時はAccepted factを作らず、responseがcommit後に遅れてもdeadlineを後ろへ動かさない。schedulerはdistinct最大32 targets / serialized 4 MiBのunresolved occupancyとdeadline scheduleを管理する。healthy storageでStopAcceptance critical manifestとterminal critical manifestへ各125 ms、1 target計250 msのservice budgetを予約し、32件のacceptance＋terminalで`32 * 2 * 125 ms = 8s`、残りをschedule余白にする。force slotを`T0 + 1.5s ... T0 + 9.25s`へEDFで割り当て、`scheduled_force_at`までにterminalが無ければruntime event lockを待たないforce-finalizerを起動し、`T0 + 9.5s`をterminal commit deadline、残り0.5秒をdirect query / emitへ予約する。`T0 + 9.5s`までに有効scheduleを確保できなければAcceptedにしない。provider / lock hangとstorage failureを混同せず、storage failure時はpre-reserved obligationを維持して通常Idleへ進めない。10秒でpending `ReconciliationRequired`へ移った場合は期限切れforce slotだけを解放し、unresolved occupancyは同じTerminalCommitの4値terminal resultがdurable確定するまで保持する。
  - startupはpublic mutation admission前にpending `TerminalCommit`の`(session, turn, runtime epoch) / audit timestamp / UTC deadline / force slot`から、`ReconciliationRequired`を含むsame-target join indexとunresolved occupancyを再構築する。persisted時刻は監査 / foreign-boot期限分類だけに使い、元のmonotonic T0を再構築したことにしない。期限到達済みまたはclock rollback等で残時間を証明できないrecordはprovider effectを再実行せずR-009の`ReconciliationRequired`へ直ちに送り、証明可能なrecordをscheduleする場合もpersisted deadlineより後へ置かずaccepted_atを再採番しない。32 distinct targets / 4 MiB超過、重複slot、target / epoch不整合ではStop admissionを閉じる。terminal result未確定のtargetは32件へ数え続け、33件目を拒否する。
  - terminal winnerは`(session, turn)` gateで一つにし、Started turnは`TurnExecution`、queued由来turnはacceptance時の`reserved_turn_id`で`QueueExecution` umbrellaをexactly one取得する。Accepted Stopがあればsame-target joinから単一`TerminalCommit`も取得する。TerminalRecord、final parts、assistant message、Session / permission / reason別queue state、umbrella result、存在するTerminalCommit result、両pending / claim / index削除、occupancy / permit releaseを一つのterminal closureで確定し、partial commitなら全pending identityを残す。そのStopの`Interrupted(User | Timeout)` winnerではumbrella / TerminalCommitを共に`Succeeded`、normal completion / Fatal / SessionClosed / 競合terminal winnerではumbrellaを`Succeeded`、TerminalCommitを`Superseded`へ進める。commit後だけoccupancyを解放し、late candidate / emit failureでwinnerを上書き・rollbackしない。
  - provider await前に得たsession revision、command generation、runtime epoch、turn idをcanonical commit直前に再検証し、late holderがterminal後のstate / parts / runtimeを上書きしない。

### I6: ユーザー入力の無損失

通常sendのAccepted dispositionはturn開始またはqueue追加のいずれかである。受理前failure、steer非対応、stall中、受理後のprovider起動失敗のいずれでも、入力テキスト・添付画像を消失させたり別operationとして暗黙再送したりしない。

- ギャップ: OB-2（stalled turn 中の送信が即エラーになり入力ごと消える）
- 要点: `steer` 未対応 backend への実行中送信は queue へ積む（現行仕様）。stalled 判定中も同じ経路に載せ、エラーにしない。送信 API が失敗した場合も入力欄の内容は保持する（presentation 側 P5）。
- 通常sendは既存の`send_agent_message` commandに、WebSocket outer request IDとは別のcaller指定`SendOperationId`とexact payloadを渡す。operation IDは1〜128 bytesの`[A-Za-z0-9._:-]`で、Tauri / WebSocketのcurrent-installation principal scope内だけで意味を持つ。exact payload bindingは現行入力の`chat_session_id / worktree_path / permission_mode / plan_mode / backend_id / model_id`、content、images、mentions、editor context、active-turn policyをcanonical bytesへ固定し、Rustがoperation identityへのbinding、validation、durable intent、receipt、status、retryを所有する。
- same principal / operation ID / exact payloadのretryは同じdecisionを返す。same operation IDに異なるpayloadを渡した場合は3 variantのsend resultやexact 19種の`SessionOperationFailureKind`へ加えず、operation IDを持つdeterministic pre-commit typed `PayloadConflict` application errorで既存state / effect 0件を返す。WebSocket / Tauriは共通`PayloadConflict { identity: Send { operation_id } }`へ写像する。canonical writer開始前に失敗した場合は`RejectedBeforeCommit`としてprovider I/Oと公開state変更を0件にし、durable operation viewを作らない。writer開始後にcommit結果を確認できない場合は`OutcomeUnknown { operation_id }`を返し、同じidentityのqueryまたはsame-payload retryで解決するまで別operationを作らない。
- `SendAgentMessageResult`は`Accepted { receipt, status } | RejectedBeforeCommit { operation_id, failure } | OutcomeUnknown { operation_id }`のclosed enumである。Accepted receiptはoperation ID、session ID、backend発行opaque `input_ref`、immutable dispositionを持つ。`AgentSendOperationView`はdurableな`Accepted { receipt, status, obligations, available_actions } | OutcomeUnknown { operation_id }`だけを返し、unknown IDは`NotFound`とする。ReconciliationRequiredはacceptance後だけ`Accepted`のimmutable receiptと`SendExecutionStatus::ReconciliationRequired`の組合せで表し、受理前failureへ戻さない。post-usecase OutcomeUnknownをtransport errorにも複製しない。
- runtime create / resumeが必要な通常sendは、acceptance manifestへ`TurnExecution(Pending)`または`QueueExecution(Pending)`とstable provider correlationを持つ`ProviderEstablish(Pending)`を同居させ、execution obligationのordered `depends_on_obligation_ids`（最大3）へestablish obligation IDを入れる。Queued acceptanceはqueue item / execution ID / acceptance時採番の`reserved_turn_id` / snapshot hash、provider start用の必須`ExternalEffectIntent`、既存runtime identity / epoch / effective configuration hashまたはpredeclared ProviderEstablish identityをclosed 2値で表す`QueueRuntimeGuard`を`QueueExecution(Pending)`へ最初から固定し、pending recordのturn IDも同じ値にする。drainはこのturn IDを使い、dependency successとguard全fieldがcurrent runtime / effective configurationまたは同じProviderEstablish resultにbyte一致する場合だけclaim / EffectReservedへ進め、immutable payloadへintent、guard、ProviderEstablish dependencyを後付けしない。acceptance後のruntime喪失、runtime / configuration generation drift、guard不一致、またはpredeclaredしていないProviderEstablishが必要ならprovider start 0件のままsame QueueExecutionをReconciliationRequired、queueをPausedへ同closureで進め、#1404のCAS付きrebase / cancelだけがnew executionを作る。#1499はPending / intent / guard / dispatch handoffだけを実装し、provider start前cancelを含むqueue cancel / CAS / rebase transitionと`CancelledBeforeEffect`確定は#1404へ残す。ProviderEstablishのObserved結果と`Terminal(Succeeded)`を同じdurable transitionで確定する前はTurn / Queue obligationをclaim / EffectReservedへ進めない。他のterminal resultやpending Failed / ReconciliationRequiredはdependencyを満たさない。crash / result unknown時も同じoperation / establish / execution / reserved turn identityとruntime guardをpending-only inventoryから回復し、Sessionやturnを二重作成しない。
- Accepted receiptのdispositionは受理時に確定した`StartedTurn | Queued`を不変に保つ。ProviderEstablishが未解決の間もreceiptは同じ値を返し、execution statusだけを`AwaitingProviderStart`にする。dependency success前にstatusを`ProviderStartReserved | Running`へ進めず、receipt dispositionからprovider startを推測しない。全dependencyがdurable `Terminal(Succeeded)`になった後、provider-start obligation / dispatch fenceを先に確定した状態だけが`ProviderStartReserved`、その予約と同じeffectがstarted handleを取得した後だけが`Running`である。別の中間statusは存在しない。
- caller operation IDとpayload bindingの照合、principal scope、receipt / status / obligation projectionはRustの同じcommand / query serviceが所有する。frontendはoperation IDを生成済みsnapshotへ対応付けるだけで、payload hash、status、failure、retry可否を再計算しない。
- `steer`対応backendでは、immutable inputと`TurnSteerRequested { steer_id, target_turn_id, input_ref }`をprovider I/O前の同じlocal atomic batchでcommitする。成功は`TurnSteerAccepted`、未適用が確定したrejectは`TurnSteerRejected`、timeout/transport切断/ack後commit失敗はprovider observation付き`TurnSteerReconciliationRequired`へ分ける。結果不明を自動再steer/queueせず、同じsteer idでrestart/reconciliationする。
- idempotencyまたはauthoritative readbackを持たないbackendはsteering capabilityを広告せず、queueへfallbackする。明示rejectをqueueへ移す場合は`TurnSteerRejected + QueueItemEnqueued`を同じbatchで確定する（L15 #1498）。

### I7: permission の有効性

permission request は「backend が回答を待っている間」だけ Pending である。CLI 側の取り下げ（Claude `control_cancel_request`）・turn 中断・turn 終端で即座に `Cancelled` に遷移し、durable / UI の両方へ反映される。解決の記録は実効性（`effective`）を区別する。

- ギャップ: CL-1（cancel 未処理で効かないダイアログが残り、誤った Allowed/Denied が永続する）、FE-1（interrupt 中に操作可能なダイアログが残る — 表示は presentation 側）
- 要点: `control_cancel_request` を処理して `PermissionRequested` を Cancelled へ更新する。失効後に届いた回答は `effective=false` で記録し、backend へは送らない。
- close / shutdown terminal closureでは未送信`Pending`だけを`Cancelled(effective=false)`へ畳む。write-ahead済み`Responding / Resolving`はCancelledへ上書きせずprovider observation付きreconciliation obligationとして保持する。
- view closeとactive中に拒否したbackend switchはpermission stateを変更しない。

### I8: 状態変更の ack 駆動と失敗の可視化

backend に対する状態変更（set_model / set_permission_mode / interrupt / permission 応答）は、backend の応答（control_response / JSON-RPC response）を検査して初めて確定する。楽観更新で UI と backend 実態を乖離させない。永続化・emit の失敗は握りつぶさず着地させる。

- ギャップ: CL-2（control_response を無言破棄し楽観更新）、CX-6（JSON-RPC error response を warn ログのみで握りつぶし）、ST-4（`let _ =` による persist 失敗の無視）、RT-4（event log が自己修復せず恒久故障）、RT-8（append 失敗時に欠落 parts で上書き）
- 要点:
  - 応答エラー時は state を巻き戻し、`Notice(level=Error)` で通知する。
  - permission回答はprovider adapter向けのexact validated response payload（`updated_input / answers / deny message`）を構築し、owner-only private blobへsyncしてrefを得る。redacted `PermissionResponseRequested { response_id, request_id, decision, persisted_answers, private_payload_ref }`と`PermissionDelivery(Pending)` obligationを同じpre-I/O manifestで確定して`Responding`へ移し、obligationを`EffectReserved`へCASした後だけproviderへ送る。ack / authoritative observation後だけ`PermissionResolved`にする。要求自体はPendingのままproviderが回答を明示rejectした場合は、理由付き`PermissionResponseRejected { response_id, request_id, reason }`をappendして旧responseを終端し、requestを`Pending`へ戻す。
  - exact private payloadはmanifest本文、eventの公開field、read model、UI、semantic hash、構造化logへ書かない。blobは共有content-addressed pathでなくrandom identityを使い、POSIX mode `0600`またはWindowsではcurrent user / SYSTEMだけのACLで作成・open時に検証する。現行app-data privacy boundary以上の暗号化を導入済みとはみなさない。effectがObservedまたはresponse / turn terminalになるまでrefを保持し、その後だけGC可能にする。
  - permission reconciliationも共通write-ahead解決sagaを使う。`ReconciliationResolutionRequested(Permission(...))`をappendして`Resolving`へ排他遷移し、ReadBackは新observation、AcceptObservedは同request/decisionの一意なaccepted/cancelled/tool-start証拠、Cancelはprovider cancel ackまたはturn終端を確認してから`PermissionResponseReconciled`で閉じる。結果不明時もprivate refからexact payloadを再構築できるが、idempotencyまたはauthoritative readback無しにblind resendしない。ReenterSecretはproviderが同requestをPendingと確認し、private payloadが安全に利用できない場合だけ旧responseをrejected terminalにして新response idを発行する。未完了resolution attemptはrestart時に同じidでreadbackし、二重回答・二重cancelをしない。
  - durable event / read model /履歴にはsecret answerを`Redacted { answered }`としてだけ保存する。private blobはexternal-effect recovery payloadであって履歴表示や一般evidence storeではない。responseが明示reject / Observed / terminalになった時点で旧refを解放し、再回答は新response idと新private blobを使う。
  - canonical mutationの`let _ =`を全廃し、persist失敗は「bounded retry → provider I/O前なら該当operationの明示failure、I/O後ならprovider observation付きdurable reconciliation」に統一する。Notice(PersistFailure)は可視化であって唯一の着地点にしない。commit後のemit / telemetryはbest-effortとして別分類する。
  - Phase 0 redo bridgeはmanifest / COW page / transaction-inventory rootのhash検証と、expected-revision root CAS後のroot file / parent / required ancestor syncが全て成功した時だけCommittedとする。visible / reachableなrootだけでは確定しない。writer開始後の結果不明はsame attemptへjoinし、resolver / restartがhash検証とrequired syncを再実行して成功した場合だけCommitted、再sync不能ならStillUnknown / store Stalledとして次mutationとprovider I/Oを止める。BeforeCommitは元writerのroot CAS前終了＋rollback ack、またはexclusive writer lockで旧writer不在を証明したrestart後fresh absenceだけで確定する。
  - Loadを含む`SessionOperationFailureFeedback`対象のdomain read / mutationはoperation開始前にprocess-wide feedback slotを予約し、成功時は解放、failure時は同じreserved identityでentryを確定する。entryは`failure: SafeOperationFailure`を一つだけembedし、kind / retryable / label / detailを別fieldへ複製せず、top-level `failure.correlation_id`をfailure/log correlationの唯一のidentityとして保持する。collectionには未解決failureだけを置きsuccessをentry化しない。512件上限で予約不能なら対象へ作用する前に`FeedbackCapacityExceeded`を返し、mutationはexternal effect 0件にする。既存未解決entryをevict / coalesceして枠を作らず、Session close / archiveでも自動削除しない。feedback getは`GetSessionOperationFeedbackRequest { session_id, cursor, limit: 1..=32 }`、dismiss / retryは`feedback_id + expected_revision`とretry時のRust-owned `action_id`を使うcapacity-exempt control planeである。dismiss成功、retry成功、retry再失敗は更新後snapshotを持つ`Applied`、unknown / stale / action不正はeffect 0件の`Rejected(NotFound | RevisionConflict | ActionUnavailable)`、control自身のstorage failureは新feedbackを作らない`Failed`で返す。dismiss成功は未解決countを1減らし、retry再失敗は同じfeedback ID / revisionをCAS更新してcountを増やさない。
  - event log の破損（欠け `]` 等）は読み込み時に自己修復し、修復した事実を Notice に残す（RT-4）。
  - **例外**: `Notice(PersistFailure)` 自体の永続化も失敗し得るため、PersistFailure に限り transient（session バナー）での表示を許容する。durable-first（L-P1）の唯一の例外として presentation 文書 P1 にも明記する。

### I9: resume 回復の統一

backend session の resume 失敗（mismatch・thread 消失）は、両 backend とも同一の回復経路を通る: transientな`BackendSessionCleared`受信 → `BackendSessionRecoveryStarted { recovery_id, old_provider_session_generation, reason }`とresume metadata clearを同じlocal atomic commitで確定 → configuration/Goalを同じ`recovery_id`の`RecoveringBackendSession`へ投影してturn/queue/workflow resumeをblock → 新規 establish → selected configurationのreapply/readbackと`SessionConfigurationReactivated`（provider session generation更新、readback observation idをconsume）→Goalの有無・terminal/currentを含めたrestore/readback/reapply。最終`SessionGoalReactivated + BackendSessionRecoveryCompleted`を同じlocal atomic batchでappendして初めてSynced/公開し、Goal restore strategyが`StartsTurn`なら`TurnStarted`もそのbatchへ含めearly streamをbufferする。Goal readback pathはconsumed observation idを保存する。結果不明はGoal/turn reconciliationへ送り、NoCurrentGoal/terminal/no-changeでも必ずGoal reactivation eventをappendする。旧effectiveを新provider sessionの実効値として使わず、回復失敗はreconciliationへ送る。

- ギャップ: SD-1（Claude は無言自動復旧・Codex は恒久死）、OB-8（requeue で editor_context が脱落）
- 要点: requeue する turn input は editor_context を含め完全に保全する。

### I10: backend stdout の頑健性統一

両backendのstdout読み取りは共通の分類契約を持つ。provider/versionで明示識別できるdiagnostic log/noiseだけをskipしてカウントし、`Notice(Diagnostic)`として可視化する。未分類の非JSON・malformed frame・size上限超過はcontent/controlを判別できないため破棄継続せず、raw refと部分protocol identityを`ProtocolIncompatible`へ保存して新規turnをblockする。「セッションを殺さない」は既存read modelを閲覧・回復操作できるままにする意味であり、send継続を意味しない。

parse可能でcontent-planeと確定できた未知message/partだけはraw payloadを保全して`Notice(UnsupportedMessage)`へ着地させる。mode・Goal・effort・approval・sandbox・reviewer等のcontrol-plane未知値/variantは`ProtocolIncompatible`または対象aggregateのreconciliationへ移す。

- ギャップ: SD-3（Codex は非 JSON 行 1 行で即死・サイズ上限なし。Claude は 8MB 破棄が type 不明のまま）
- 要点: 読み取りラッパを infrastructure 共通部品にし、両 backend で使う。

### I11: 生存シグナルと stall 判定

「進行中」の判定は backend からの進行シグナル（parts delta / KeepAlive / status 変化）の有無で行い、シグナル要件は backend 別に定義する。長考（reasoning）中も何らかのシグナルが届くこと（Codex は reasoning delta 購読 — CX-3 の解消 — で充足）。stall 診断は「実際に進行が観測できない」場合のみ発火する。

- ギャップ: SD-4（Codex reasoning 中の無シグナルで stall 誤検知）、ST-9（permission 待ち以外の不可視停止に診断が無い、閾値 60s）
- 要点: stall 診断を全 phase（Streaming / WaitingPermission / Interrupting）に拡張し、`Notice(Diagnostic)` として durable に残す。

### I12: エラーの着地保証

backend プロセス死・turn 失敗・Fatal は、発生時点で durable な `Error` part または `Notice` として着地し、live UI にも即時反映される（表示規則は presentation P3）。workflow への turn 完了通知は構造化された失敗理由（`TurnError`）を運ぶ。

- ギャップ: RT-6（Idle 中 Fatal が痕跡ゼロ）、FE-2（crash/timeout が live で無言 — 表示側）、RT-5（workflow に exit 1 しか伝わらない）

### I13: 排他とロックの規約

session runtime lock の保持中に、別 session の lock・長時間 await（backend I/O）を行わない。lock の取得順序と保持範囲を規約として明文化し、prune はランタイムハンドル取得に依存しない方式にする。

- ギャップ: ST-5（二段ロックと prune skip）、ST-3（巨大 module 内で lock 範囲が追えない）

### I14: Agent 実行設定の ack・revision 保証

provider/model、mode、reasoning effort の変更は Rust usecase が所有する。Goal は I15 の独立 aggregate とし、configuration に Goal 参照を持たせない。外部 provider と local persistence の atomicity を仮定せず、`durable intent → adapter apply / ack → canonical event commit → activation` の順で処理する。

- `AgentSessionConfiguration` aggregateとcanonical configuration eventがmutation/durable authorityを所有する。`AgentSessionConfigurationProjection`はそれらから構築するquery read modelであり、selected intent、effective provider state、1件のpending update、sync stateに加え、model / mode / effort候補ごとの`available_actions`を持つ。Rust queryはIdle、configuration / Goal sync、pending update / transition、control-operation lease、runtime capability、managed policyから`enabled / reason / update_timing / requires_bypass_challenge`を評価する。selected effortとeffective / unknown effortを別型にし、provider/modelを同じrevisionへ含める。
- command は full snapshot でなく `ConfigurationPatch` の1 variantを送る。model変更だけは対象modelとmodel-bound effort selectionを同じsemantic patchに含め、`target_model.provider_id == session.provider_id`をRustで必須検証する。provider変更は通常patchではなく、turn finalize、Goal/queue handoff判断、protocol preflightと新launchを伴う別usecaseとする。Rustがbase selected snapshotからtarget snapshotを導出する。
- execution-affectingなuser更新は初期実装ではIdleのみ受け付け、Session共通control-operation leaseを取得する。`base_selected_revision`をCAS検証し、configuration/Goalのいずれかが非Synced、Goal transition pending、別update/lease中ならprovider I/O前にconflictを返す。Streaming / WaitingPermission中のmode変更や、configuration activation中のClaude Goal StartsTurnを許さない。
- provider I/O前に`ConfigurationUpdateRequested { update_id, base_selected_revision, target_revision, patch, applies_from }`をevent logへappendする。append成功がdurable intentのcommit pointで、idempotenceは`update_id`で判定する。
- liveのprovider ack、またはnext-turn/restart stagingのtyped adapter acceptance後の`SessionConfigurationSelected` event appendをselected configurationの唯一のdurable commit pointとする。staging acceptanceはprovider effectiveを意味しない。SessionMetaはeventから再構築可能なprojection/cacheであり、その更新失敗は`PersistFailure`と再投影で回復する。
- live activationは`SessionConfigurationActivated` append後だけeffective revisionを進める。独立configuration APIがある`AwaitingNextTurn`はprovider turn開始前にselected patchを適用し、activation ackとevent append後にeffective snapshotでturnを開始する。`AwaitingRestart`もrestart/readback後に同じ順序でactivateする。
- providerがmodel/mode/effortを`turn/start` payloadでしか受けない場合、`TurnStartRequested`は実行sourceを区別する。既存effective/queueは`ExistingEffective(ResolvedTurnConfiguration)`としてcanonical semantic snapshotとsource hashを固定し、pending selectedはprovider ack前の値をeffectiveと呼ばず`ActivateSelected { selected, originating_update_id, canonical_target_hash, prevalidated_context_hash }`として保存する。queue起点ではcurrent Session selectedへjoinし直さない。通常sendはhuman message記録、queue/Bypassはqueue execution intent/challenge consumeと同じlocal batchにする。early eventをbufferし、ack/readback後に初めてactual effective snapshotを`TurnStarted`へ固定する。`SessionConfigurationActivated`は`ActivateSelected`をsession-scopeで実際にactivateした場合だけappendし、queue per-turn overrideでは`QueueItemStarted + TurnStarted`だけをatomic commitしてSession effectiveを進めない。ack/commit結果不明はprovider turn/config observation付き`TurnStartReconciliation`へ送り、未完了intentはrestart後もblockする。
- TurnStart reconciliationのReuse/AcceptObserved成功はconfiguration activation、`TurnStartReconciled + TurnStarted`、queue起点なら`QueueItemStarted`（provider側が既に完了ならTurnResultも）を同じlocal atomic batchで確定する。Cancel/CleanUp成功は`TurnStartReconciled + QueueItemFailed/Cancelled + message marker`をatomic appendする。どのactionでもqueue itemを`Starting`のまま残さず、結果不明は同じresolution attemptでblockを継続する。
- pending updateはActivated / Rejected / Reconciledまで保持し、`Synced`以外では次の更新を受け付けない。`SessionConfigurationSelected`前のprovider rejectだけは`ConfigurationUpdateRejected`を記録してpendingを消し、旧selected/effectiveを維持する。selected commit後のNextTurn/Restart activation reject・timeoutではnew selected / old effectiveを維持してreconciliationへ入り、selectedを旧revisionへ戻さない。rollbackを選ぶ場合は旧effective相当を新しいselected revisionとしてcanonical appendする。
- timeout、部分成功、provider ack後のcanonical event append失敗、provider-originated競合は`ConfigurationReconciliationRequired`とし、新規turn / queue drain / workflow resumeをblockする。各reconciliationに新しい`reconciliation_id`を発行し、local request由来のときだけ`originating_update_id`、provider観測があるときだけ`observation_id`を関連付ける。provider-originated driftのために架空のupdateを作らない。`ProviderConfigurationStateObserved`、差分、allowed actionsをdurable化し、readback / idempotent reapply / rollback /明示acceptを`ConfigurationReconciled`で確定する。未完了request自体もrestart後のreconciliation根拠になる。
- `ProviderConfigurationStateObserved`のappend時点でsync stateを`ObservationPending { observation_id }`にしてblockする。同じobservation idをcanonical activation/no-change acceptance/reconciliation eventがconsumeするまでSyncedへ戻さず、restartは未consumed observationを必ず再評価する。
- configuration / Goal / launch / turn-start / permissionのreconciliation解決は、expected observation id/projection seqをCAS検証してresolution attempt idをreserveする。Bypassを結果にする場合はscope/reconciliation/attempt/observation/seq/action/target hashへ束縛したfresh challengeを発行する。`ReconciliationResolutionRequested { resolution_attempt_id, reconciliation_id, action, target_hash }`とchallenge consumeをprovider I/O前の同じbatchでappendし、`ResolvingReconciliation`へ排他遷移する。ack/readback後だけ`*Reconciled`をappendし、未完了intentはrestart時に同じattempt idでreadback/recoveryしてReapply/Recreate/cleanup/Claude Goal turn/permission responseを二重実行しない。
- initialize時にspawnしたbinaryの`BackendProtocolIdentity`とcompiled schema/flags/capabilitiesを照合する。不一致やcontrol-plane decode failureは`ProtocolIncompatible`としてfail-closedにする。initialize完了前は取得済みfieldだけの`ObservedProtocolIdentity`、expected hash、raw control refをlaunch attemptへ保存する。
- pre-session `ProtocolIncompatible`でもprovisional resourceがあり得るため、statusは`reconciliation_id`付き`LaunchReconciliation`を内包する。cleanup/cancel/reuseは共通`ReconciliationResolutionRequested` sagaを通し、protocol errorを理由にwrite-ahead回復を省略しない。
- New Agentのsubmit前はworkspace/provider/context keyed preflight queryが`Checking | Compatible(capabilities) | ProtocolIncompatible(partial identity)`を返す。`prepare_agent_launch`がdraft検証後にattempt id/canonical draft hashをreserveし、Bypassなら`AgentLaunchDraftPrepared + BypassChallengeIssued`を同じlocal atomic batchでappendする（non-BypassはPrepared単独）。`start_agent_launch`は`StartAgentLaunch.bypass_confirmation`のchallenge id / nonceをIssued challengeへ照合し、reserved id/hash/preflight context、期限、guard、caller scope、workspace trust、managed policy、provider identity/capability/gateを再検証できた場合だけBypass consume＋`AgentLaunchAttemptStarted`をatomic appendしてからprovider I/Oする。attempt id / draft hashだけではconsumeできない。draft変更/期限切れはreservationを失効させ再prepareする。
- 初回createはvalidated `AgentConfigurationDraft`と`canonical_draft_hash`からdurable `AgentLaunchAttempt`を発行する。`attempt_id`から安定生成したprovider create correlation keyと、provider対応時のidempotency keyをrequest前に保存し、provider session/threadをprovisionalに作成する。各`StageAdvanced`はprovider ref、local session id等のpayloadも保存する。途中失敗はcleanupし、成否不明ならlaunch reconciliationへ移す。readbackはcreate lookupのFound/NotFound/Ambiguous/Unsupported、match basis、lookup consistency/stable-since、provider ref/configurationをdurable化する。Reuseは一意Found、Recreateはauthoritative NotFoundまたは同じkeyで冪等createを保証できる場合だけ許可し、eventual NotFound単独では許可しない。reserved attempt別event streamと`get_agent_launch / watch_agent_launch(after_seq)`、単調`seq`を正本とする。watchは`open_watch`がcursorのreplay可否、required sourceのcommon watermarkへpinしたsnapshot lease、barrier後subscription/receiverを同じstorage transaction / commit lockで確定する。usecase-owned watch serviceはsnapshot/replayにはhandleのlease、live更新には各notice commitへ厳密にpinしたfenceの`read_at`だけを使い、full `AgentLaunchChanged`を構築してから`finish_bootstrap / finish_update`する。古いcursorはfull projectionを返し、bootstrap中・projection追随待ちの通知はbounded bufferへ保持する。buffer超過、lease失効、`ProjectionBehind`、gapでは`close_watch`して部分結果を捨てて再openし、disconnect時も`close_watch`してget→subscribe race、field欠落、subscription leakを作らない。frontend draftは正本ではない。
- provider resource作成とinitial configurationのapply/readback後、`SessionCreated + SessionConfigurationSelected(revision=1) + SessionConfigurationActivated(revision=1) + LaunchStageAdvanced(LocalSessionCommitted)`をlaunch/session stream横断のlocal atomic batchでappendし、new Sessionのconfigurationをseedする。initial Goalなしは`AgentLaunchCompleted`も同じbatchへ含める。batch失敗ではSessionを公開せずlaunch reconciliationへ入り、SessionMetaは成功batchから後で再投影する。
- draftにinitial Goalがある場合もGoalはconfiguration/launch provider createへ混ぜない。local Session commit後、`LaunchStageAdvanced(InitialGoalTransitionRequested)`と`GoalTransitionRequested { originating_launch_attempt_id }`を同じlocal multi-stream atomic batchでappendしてからprovider I/Oする。canonical Goal event、Claudeの`TurnStarted`、`LaunchStageAdvanced(InitialGoalCommitted)`、`AgentLaunchCompleted`も同じatomic batchで確定する。それまでは`WaitingForInitialGoal`とする。結果不明時はattemptからGoal reconciliationを参照し、restart/reuseでも同じtransition idを再開する。明示rejectはGoal streamの`GoalTransitionRejected`とlaunch streamの`LaunchInitialGoalRejected`を同じatomic batchでappendし、attemptを`WaitingForInitialGoalResolution`へ再投影可能にする。
- reject解決はexpected transition/attempt seqをCASし、`InitialGoalResolutionRequested`をappendして`ResolvingInitialGoalFailure`へ移す。RetryGoalは`InitialGoalResolutionCompleted { action: RetryGoal, next_transition_id }`＋新transition idのlaunch/Goal intentをatomic appendして`WaitingForInitialGoal`へ移す。ContinueWithoutGoalはresolution completed＋launch completed、CancelSessionはcleanup intent後にresolution completed＋launch cancelled＋session closedをatomic appendする。cleanup結果不明は同resolution attemptのlaunch reconciliationへ送り、未完了resolutionをrestart時にreadbackして暗黙retryしない。
- `BypassConfirmationChallenge`はtarget、期限、nonceに加え、Sessionなら`session_id + selected_revision`、Launchなら`attempt_id + canonical_draft_hash`、Workflowならshared-kernelの`NodeExecutionRef + resolution_id + resolved configuration hash`へ束縛する。Workflow Bypassは`NodeExecutionBypassPrepared + BypassChallengeIssued`をatomic appendして`WorkflowWaitingBypassConfirmation`へ移し、reload後も同じ期限・guardを表示する。失効時は`BypassChallengeExpired`から`BypassConfirmationExpired`へ投影して新prepareを要求する。Sessionの`SetMode { mode: Bypass, bypass_confirmation }`、Launchの`StartAgentLaunch`、Queueの`QueueExecutionStartIntent`、Workflowの`NodeExecutionLaunchIntent`はchallenge idとnonceを対で受け取る。consume usecaseは提示nonceを当該Issued challengeのnonceと照合し、`expires_at`、guard、認可済みcaller/session/workspace scope、managed policyを全て再検査できた場合だけ、`BypassChallengeConsumed`を`ConfigurationUpdateRequested`、`AgentLaunchAttemptStarted`、`QueueExecutionRequested`、`NodeExecutionLaunchRequested`の各intentと同じlocal atomic batchでappendする。nonceはIssued中の認可済みclientだけが持つsecretであり、challenge idとobservableなattempt id / draft hash / snapshot hashだけをbearer proofとしてconsumeしてはならない。provider I/O中にlockは保持せず、失敗後もchallengeはconsumedのままにする。同一intent id・同一guardのidempotent retryだけを許し、新しいintentには新しいchallengeを要求する。Claudeのdangerous launch opt-in等provider gateも別に必要で、gate無しSessionではrestart-requiredまたはdisabledとする。template保存だけではBypassを付与しない。
- `send_agent_message`はSessionのeffective snapshot、queue drainはI4のimmutable snapshot、workflow起動はdurable baseline/default/overrideからRustが解決したsnapshotを使う。frontendから毎turn設定を再送して正本を上書きしない。
- ただし全`TurnStarted`、queue drain、backend resume、workflow resume直前にprovider/model/mode/reasoning effortと必要なGoal continuation capabilityを、最新のdeployment/org override、workspace trust、managed policy、provider gate/residual protectionsを含むavailability context hashでRustが再評価する。effortのauthoritative validationができない、またはeffective Bypassのpolicy/gateが設定後に失効した場合は送信せずUnknown/NeedsResolution/reconciliationへ移し、silent clampや必要なprovider restartの隠蔽を禁止する。成功時のprovider permission/effects/residual protections/context hashはimmutable`EffectiveModeSnapshot`としてexecution configurationと`TurnStarted`へ固定する。
- legacy / unknown設定はvocabulary V-D10のmigrationを通す。復元不能ならscope/field/raw payload/resolution id/actionsを持つ`NeedsConfigurationResolution`としてblockし、`Edit`等へfallbackしない。

### I15: Goal lifecycle と回復保証

Goal は configuration とは独立した `goal_id / goal_revision / pending transition / sync state` を持つ Rust-owned aggregate とする。Session ごとのcurrent Goalは最大1件で、terminal Goalもclearまたは次のsetまではcurrentとして保持する。provider eventは`goal_id`またはopaque provider refで相関し、古いGoalの遅延通知をcurrent Goalへ適用しない。

| 現在 | 入力 / actor | 次 | 規則 |
|---|---|---|---|
| None / Completed / Failed | set（User） | Active | 新しい `goal_id` を発行。既存 Goal を再利用しない |
| Active / Paused | edit（User） | 同 status | objective と goal revision を更新。provider / evaluator へ再適用 |
| Active | pause（User / Provider） | Paused | native 非対応時は capability に明示した clear 等で emulation |
| Paused / Blocked | resume（User / System） | Active | capability 再検証後に再接続 |
| Active | blocked（Provider / System） | Blocked | reason、normalized status、raw provider evidence refを保持 |
| Active | completion（Provider / Evaluator / System） | Completed | evidence ref 必須。turn 停止だけから推測しない |
| Active / Paused / Blocked | failure（Provider / Evaluator / System） | Failed | failure reason を保持 |
| Active / Paused / Blocked | clear（User / System） | None | `GoalCleared` を記録。Completed を合成しない |
| Completed / Failed | clear（User / System） | None | terminal outcome の表示を明示的に閉じる |

- user起点のset/edit/pause/resume/clearはIdleかつconfiguration/GoalともSynced・pending無しの場合だけ受け付け、Session共通control-operation leaseを取得して`base_goal_revision`をCAS検証する。provider I/O前に`GoalTransitionRequested`をappendし、ack後の`GoalSet / GoalTransitioned / GoalCleared` appendをcanonical commit pointとする。rejectは旧currentを維持し、timeout/部分成功/ack後append失敗/provider競合はGoal専用reconciliationにする。各reconciliationに新しい`reconciliation_id`を発行し、local request由来のときだけ`originating_transition_id`、provider観測があるときだけ`observation_id`を関連付ける。provider-originated observationのために架空のtransitionを作らない。
- pending transitionは`GoalSet / GoalTransitioned / GoalCleared / GoalTransitionRejected / GoalReconciled`のいずれかまで保持する。成功終端はcanonical Goal eventだけで、別の`GoalTransitionApplied`を設けない。`SessionGoalProjection.available_actions`はstatus、capability、pending state、managed policyからRustが評価し、frontendは遷移表を再実装しない。
- `ProviderGoalStateObserved`のappend時点でGoal sync stateを`ObservationPending { observation_id }`にし、provider refとMatched/Unmatched/Ambiguous相関をsnapshotへ保存する。同じobservation idをcanonical/no-change/reconciliation eventがconsumeするまで新規turnをblockし、未consumed observationはrestart時に再評価する。
- capability はset/edit/pause/resume/clear等の操作ごとにstrategy、application scope、effectsを含む`Native / Emulated / Unsupported(reason)`を返し、schema supportとは別にworkspace trust/session/managed-policy contextでruntime availabilityを評価する。provider RPC、provider CLI command、明示的なReleash-managed evaluatorを分け、暗黙のprompt接頭辞で対応済みに見せない。
- Claude `/goal <objective>`は`ProviderCliCommand`であり、setまたはactive Goalのeditと同時にturnを開始する。Setは`StartsTurn`、Editは`StartsTurn / ReplacesProviderGoalIdentity / ResetsProviderProgress`を宣言する。acceptanceはpinしたCLI fixtureで証明した`system/command_lifecycle(completed, command_uuid)`とtyped Goal state (`goal_set`/`goal_status`またはactive Goal snapshot)の両方、かつ要求objective hash一致を必要とする。Goal intent→command後はcontent-plane deltaだけをbufferし、evidence observation＋`GoalSetまたはGoalTransitioned + TurnStarted`を一つのatomic batchでappendしてから公開する。commit前の`can_use_tool`/`request_user_dialog`等の応答必須control-planeはbufferせずfail-closed応答→interruptし、raw request/response/interrupt evidence付きGoal/turn reconciliationへ送る。shape/order/相関をfixtureで証明できないCLI versionやworkspace trust/hooks/managed policy不明ではStartsTurn actionをadvertiseしない。このprotocolはinitial Goal、RetryGoal、resume emulation、backend recovery中のGoal restoreにも共通適用する。
- Claude Codeの`--resume / --continue`によるSession復元はGoal ActionのResumeではない。復元されたGoal stateとaccounting baseline resetを観測してもturn開始を合成しない。pauseをclearでemulateした後のGoal Resumeを`/goal <objective>`再setで行う場合だけ、`StartsTurn / ReplacesProviderGoalIdentity / ResetsProviderProgress`を宣言する。Codex objective editもset RPCによるidentity置換・progress/accounting resetをeffectとして公開する。
- clear/re-setによるpause/resume emulationがprovider進捗reset、turn開始、provider Goal identity置換を伴う場合、そのeffectsを操作前に表示する。補償不能ならUnsupportedとする。
- Codex statusはactive/paused/complete/blocked/usageLimited/budgetLimitedを全域写像する。unknownはadaptor/gatewayのraw evidenceを保持し、domainの`ProviderGoalSnapshot.normalized_status=None`と`ProviderEvidenceRef`からreconciliationへ進める。FailedはReleash/System側statusとする。
- close / crash / app restart / backend resume / workflow restart を跨いでも同じ Goal id、objective、status、goal revision を復元する。Claude の active Goal resume や Codex readback など provider 固有の回復を adapter が担う。
- Goal の automatic continuation は `AgentMode::Auto` と別概念である。どの mode でも permission と workflow human checkpoint を維持し、Plan / provider policy が continuation を制限する場合は理由付きで停止する。
- workflow の `task` は step の作業指示、Agent Goal は Session の継続的な completion condition / status であり、別フィールドとして保持する。片方から他方を暗黙生成しない。
- provider 固有の Goal budget / usage accounting はnormalized snapshotとraw evidence refによる停止理由の表示だけを行い、今回の Goal 設定にも ReasoningEffort にも取り込まない。
- clear/replace後もturn監査から当時のobjectiveを引けるよう、Goal canonical eventからpaged historyと`goal_id + revision` lookupを提供する。transition recordはkind/result/time、before/after snapshot、source/evidence、launch attempt相関を持ち、current projectionへ全履歴を保持しない。

### I16: ReasoningEffort capability と反映時点の保証

Reasoning effort の選択肢・説明・既定値・反映時点（live / next turn / session restart）は provider/model capability から取得し、provider が広告した順序のまま read model に含める。runtime capability APIがないproviderではprotocol identityにpinしたRust compatibility tableをsourceとして明示し、検証不能なmodel/valueはUnsupportedにする。

- selectedは`ProviderDefault / Explicit(value)`、effectiveは`Known { value, source } / Unknown { selected, expected?, reason }`として分ける。default、未取得、非対応、effective不明を`Option`一つに畳まず、tableの予想値をprovider確認済みeffectiveとして扱わない。
- 選択値が model に非対応なら更新をprovider I/O前に拒否し、利用可能な値と理由を返す。model変更patchはtarget modelとそのmodel向けeffort selectionを一体で検証し、別値へsilent fallbackしない。Claudeはprovider/model/deployment、organization上限、capability overrideまで含む実行contextでauthoritative validationまたはeffective readbackができなければ明示effortをUnsupportedとし、static tableや下位levelへのsilent clampに依存しない。
- next-turn は selected を確定して `AwaitingNextTurn`、restart 必須は `AwaitingRestart` とし、activation event まで旧 effective revision を使う。次turn開始前にactivationを完了し、失敗時は`TurnStarted`を記録しない。reconnect 後も pending / awaiting state を durable intent から復元する。
- Plan preset 等が effort を内包する場合、明示選択値を上書きせず、override 可否を capability として返す。両立不能なら mode / effort conflict を解決するまで turn を開始しない。
- Reasoning effort は provider / model の応答・推論強度を調整する behavioral signal で、実使用量や上限を保証しない。`TokenUsage`、cost、経過時間、turn 数、token / cost / time budget を停止条件や代替値として混ぜない。`TurnStarted`にはprovider/model/effective effort/unknown reason/protocol identityを保存する。

#### Workflow / launch configuration の解決

workflow definition はprovider-ack済みSession configurationを直接保存せず、revision付き`AgentConfigurationTemplate`を持つ。必須model/modeは`RequiredOverride::Inherit | Set`、optional reasoning effort/initial Goalは`OptionalOverride::Inherit | Set | Clear`とし、必須値に意味不明なClearを許さない。

- Rust usecaseはmanaged policyとprovider/model capabilityから`LaunchConfigurationBaseline`を解決し、`baseline → WorkflowExecution default → NodeDefinition override`の順で`ResolvedLaunchConfiguration`を作る。effortのClearは`ProviderDefault`、GoalのClearはNoneを意味する。各fieldにprovenance、baseline/workflow execution default/node definition override revision、resolution id/versionを持たせ、canonical hashを算出する。
- WorkflowExecution開始時はbaseline、default、解決規則versionを`WorkflowExecutionStarted`へappendし、これをdurable commit pointとする。WorkflowExecution metadataはprojectionである。NodeExecution開始時のresolved snapshotもeventとして記録し、provider adapterが受理した後に初めてSession selected/effective configurationになる。
- NodeExecutionがNew Agentをlaunchする場合、全modeで既存の`NodeExecution.id + resolution_id`からstableなAgent launch `attempt_id`を導出し、originへ`WorkflowExecution.id / NodeExecution.id / NodeDefinition.name（node_name）/ NodeExecution.attempt`を保存する。`NodeExecutionLaunchRequested + AgentLaunchAttemptStarted { origin: NodeExecution(...) }`をworkflow/launch stream横断のlocal atomic batchで開始する。Bypassだけは先に`NodeExecutionBypassPrepared + BypassChallengeIssued`で`WaitingBypassConfirmation`へ進め、確認後の開始batchへconsumeを追加する。non-Bypassはchallenge無しで共通開始batchへ進む。launch完了時の`AgentLaunchCompleted`と`NodeExecutionAgentBound { attempt_id, session_id }`、失敗/取消時の`AgentLaunchFailed/Cancelled`と`NodeExecutionAgentLaunchFailed/Cancelled { attempt_id, reason }`をそれぞれ同じbatchにする。crash/restart時はorigin相関から既存attemptをresumeし、retryは新しい`NodeExecution.id / attempt`を発行してprovider/sessionを二重作成しない。
- workflow `task` と initial Goal spec は別フィールドである。Goal を作る場合は新しい `goal_id` を発行し、既存 Session Goal の暗黙継承・復活を行わない。
- templateにBypassを保存しても権限付与ではない。各NodeExecution attemptでmanaged policyを再検査し、既存の`WorkflowExecution.id + NodeExecution.id + NodeDefinition.name（node_name）+ NodeExecution.attempt + resolution_id + canonical resolved hash`へ束縛したone-time challengeとClaude launch opt-in等provider gateを検証する。失効・consumed・異なるattempt/configurationのchallengeを再利用しない。
- legacy WorkflowExecution / external execution restoreに必要な設定が無い、または現providerで非対応になった場合はresolution id、field、reason、allowed actionsを持つ`WorkflowWaitingConfiguration`へ遷移する。`Edit`固定へfallbackしない。

### I17: close / quit surface authority と bounded shutdown

close、view close、configuration transition、application quitの意味論は[close-quit-decision-table.md](close-quit-decision-table.md)を唯一の正本とする。controllerはcommand入口とRust-owned metadataからtyped ingressを決定し、frontendが渡す文字列や識別不能なnative eventから意味論を推測しない。

- shutdown projectionのclosed phaseは`Preparing | Prepared | Activated | Quiescing | Completed | Failed | Cancelled | ReconciliationRequired`である。new flightにeligibleなprior latestは`Completed`、またはdurable `Failed | Cancelled` terminal fenceと、同じplan / epoch、target / completed / unresolved / preexisting counts、safe failure、exit intentを持つeffect 0件の`AbortedBeforeActivation` terminal rootが揃う場合だけである。pre-activation terminal closureは`details=Available`のままfull page / target authority / snapshot refを保持し、archive / detach / GCを同closureへ入れない。既に`details=Compacted`なら`ShutdownPlanCompactArchiveV1`とのparityを検証する。全caseでadmission Open、store Healthy、unresolved shutdown scope fence 0を同snapshotで必須とし、process-only Failed、Stalled / unhealthy、admission Closed、scope fence残存をexplicit retryで迂回しない。
- shutdown snapshotの公開上限は1 page 128 targets / encoded 1 MiB、全体4096 targetsである。129件目のpage entry、1 MiB超、4097件目は`ResponseTooLarge`または`CapacityExceeded`としてpre-activation、new plan detail / provider・workflow・OS effect 0件で拒否し、上限ちょうどまでは受理する。物理page / pack配置はD3設計に閉じる。
- plan-level `RetryQuit`はsame-boot pre-activation Failed、external effect 0件、durable Failed fence、admission Open、store Healthy、unresolved shutdown scope fence 0を同snapshotで満たす場合だけ提示する。process-only Failed、Stalled、admission Closed、scope fence残存、fresh boot、他phaseでは空であり、明示retryだけでguardを迂回しない。

- application quitは単一のRust-owned shutdown coordinatorを通り、最初のrequestでplan ID / epochを発行してAgent / Workflow / local API / Tauri mutationのglobal admissionを閉じる。same-boot current nonterminal flightはmode / codeが異なっても専用closureより先に同resultへjoinする。process flightが無い場合、専用`shutdown-init/v1` closureはprior latest plan state / root / revision、unresolved shutdown scope-fence root、new-flight admission / store / explicit retry guard、retiring pointerをこの順でfresh preflightする。prior latestがnonterminalまたはscope fenceが1件以上ならexact `PreviousShutdownReconciliationRequired`でnew plan identity / root / page / effect 0件のBeforeCommit rejectionにする。prior latestがterminalで全new-flight guardを満たす場合だけcompaction guardへ進み、details AvailableかつretiringがNoneなら旧latest→retiring、Absent rootからrevision 1のminimal Preparing root作成、`LatestShutdownAttemptRefV1`のnew planへのadvanceを同時に確定し、eligible terminal prior latestがdetails AvailableかつretiringがSomeならexact `PreviousShutdownCompactionPending`でnew plan identity / root / page / effect 0件のBeforeCommit rejectionにする。eligible terminal prior latestがCompactedならarchive / compact root / summaryのidentity、intent、counts、safe failure parityを検証し、retiring pointerを変更せずnew root / latest-attemptを確定する。root hashを返せた時だけquitをAcceptedにする。同一attempt中の重複requestは同じresultをawaitし、Session terminal、workflow shutdown、child termination、process exitを重複実行しない。closure scopeは個別`Session`、個別`Workflow`、それら全targetを束ねる`Application` shutdown planに分ける。Preparing root、complete Prepared root、以後の全state transitionは同じclosure commitでlatest-attempt pointerを単調CASし、latest activated pointerとは役割を分離する。init commit前だけがprocess-onlyでありAccepted / Preparing projectionを作らない。
- 全quit ingressはadaptorでwire DTOをdecodeした後、coordinator admission前にcanonical `ShutdownExitIntent { mode: Exit | Restart, code: i32 }`へ正規化する。caller指定request IDは1..=128 ASCII bytesの`[A-Za-z0-9._:-]`だけを受理し、不正値は`InvalidRequest`としてoperation / state / effectを0件にする。同じprincipal / request ID / same intentはsame result、same request ID / different intentはWebSocketの`AgentSessionWsErrorV1::PayloadConflict { identity: ApplicationQuit { request_id } }`またはTauriの`RequestApplicationQuitApplicationError::PayloadConflict { request_id }`＋effect 0件とする。別request IDでcurrent flightへ到着した後続quitはfirst accepted intentへjoinする。最初のintentをplan、coordinator、summary、projection、one-shot permitまで不変に保持し、pre-activation effect 0 abort後のnew flightだけがnew intentを採用する。`ShutdownExitIntentV1`はadaptor DTOに限定する。
- coordinatorはquit開始時にopenなactive / Idle Sessionと進行中Workflowだけをshutdown targetとして固定し、stable orderのimmutable pageへ分割する。1 pageは最大128 targets / encoded 1 MiB、全体は最大4096 targetsである。関連runtime / child processは`OwnedShutdownScopeRef`でowner targetのsubordinate effectとして扱い、別targetへ重複計上しない。closed / archived Sessionとdurable OrphanRuntimeの既存recoveryはtargetにせず、planが固定するpending recovery snapshotへ残す。各target pageはstable target key、ordinal、deterministic obligation ID、expected owner revisionを持ち、exact shutdown commandはbackend authorityだけが所有する。各page closureはpage / target authorityだけをimmutable staging commitし、Preparing root / latest-attemptを変更しない。complete Prepared planまではpartial pageを公開せず、Failed / Cancelledのpre-activation closureはeffect 0件の`AbortedBeforeActivation(details=Available)` summaryへ畳み、rootから到達可能にした成功済みpage、target authority、snapshot refを保持し、target countはそのpage ref count合計と一致する。
- target keyは`LP("application-shutdown-target/v1") || LP(target tag) || LP(stable owner id)`のSHA-256をbase64url no-padにした値、obligation IDは`LP("application-shutdown-obligation/v1") || LP(plan_id) || epoch(u64 BE) || LP(target_key)`のSHA-256をbase64url no-padにした値で、`LP`はUTF-8 raw bytesのu32 BE length prefixである。pageはplan ID / epoch / page index / first global ordinal、entryはtarget key / global ordinal / obligation ID / expected owner revision / authority digestを必須とし、page refもfirst ordinalを持つ。enumeration、ordinal、page first / last、public cursorはtarget keyのUTF-8 byte順へ統一し、page index / ordinalを0から連続、keyをstrictly increasingにする。root / page / refのplan / epoch / hash / countとderived key / IDを検証した後、authorityを1 key lookupし、そのdigest、plan / epoch / page index / key / ordinal / obligation ID / expected revisionをpage build・root validation・reservation / action・F3 importで再検証する。authorityのSessionClose scope / effectはboth Someまたはboth Noneで、SomeならSession owner / Runtime effect owner / runtime epochを一致させる。Workflow authorityはtarget / owner / payload / executor scope / effect ownerのworkflow execution IDを全て一致させる。authorityの欠損または不一致をpageやcurrent ownerから補完せず、effect前にtyped rejectまたはquarantineする。
- pending 3-treeとobligation-state-by-ID treeはdomain-separated child hash / subtree countを持つcomposable Merkle COW nodeを使い、insert / move / deleteごとに変更pathだけをO(path)再hashする。partition / range snapshotは境界pathと完全被覆subtreeから合成し、全ordered leafのO(N)再列挙・再hashをquit pathへ入れない。
- cross-plan duplicate gateはdomain-separated `(ObligationOwner, exact OwnedShutdownScopeRef)` keyのdirect `UnresolvedShutdownScopeFenceV1` / COW indexをauthorityにする。shutdown reservationはfence AbsentをguardしPending EffectReserved obligationと同commitでinsert、Pending ReconciliationRequired / Failedでも保持し、compact Terminal transitionと同commitでdeleteする。new flight root-initはfence root count 0をprior latest state guardと同じsnapshotで必須にし、既存entryがあればnew plan identity / root / page / effect 0件のexact `PreviousShutdownReconciliationRequired`と解決操作を返す。root-init後のtarget point lookupとcomplete Prepared root commitはそのfence-root revision / hashをguardし、Accepted後に新たなfenceが現れた場合だけeffect 0件のpre-activation abortへ進む。Phase 0はold obligationのnew planへのhandoff / reuse / association付け替えを禁止し、provider / workflow authorityで新しいruntime / executor generationを証明した別keyだけをdistinct effectとして扱う。Phase 0 direct＋COW fence rootはF3の同canonical UNIQUE keyへparity importし、不一致ではcutoverしない。
- 全pageを検証した後だけinitial `Preparing` rootをordered page hash / page count / target countを持つ`ShutdownPrepared` rootへ進め、同じclosureで`LatestShutdownAttemptRefV1 { plan_id, epoch, root_sha256, state=Prepared, coordinator_boot_id, pointer_revision }`を単調CASする。以後のroot transitionも同pointerを同commitで更新する。global activation transactionはlatest-attempt pointerのsame plan / epoch / Prepared root hash / revisionをguardに、rootの`Prepared -> Activated`、latest-attempt pointerのActivated更新、global monotonic `LatestActivatedShutdownPlanRefV1 { plan_id, epoch, activated_root_sha256, coordinator_boot_id, global_deadline_wall_ms, pointer_revision }`更新を一つのcommit pointで確定する。late old epoch、Failed / Cancelled fence後、別root revisionのactivationはlatest-attempt CASで拒否する。全target prepareのfailure、またはactivationがwriter開始前 / rollback ack済みの`BeforeCommit`と確定した場合だけeffect 0件でquitをabortしadmissionを再開する。activation writer開始後の`OutcomeUnknown`はabsenceからabortへ格下げせず、admissionを再開しない。current processは明示shutdown command 0件のまま同attemptをReconciliationRequiredとして15秒以内にexitし、fresh bootが同じactivation identityをresolveする。部分prepared page / rootとabort後のlate Preparedはcurrent coordinatorに対してinertで、startupもそこからeffectを開始しない。
- Activated確定後だけcurrent coordinatorがSession closureとWorkflow shutdownを並列開始できる。reservation closureはcurrent Activated root、page plan / epoch / hash、target key / global ordinal / authority digest、point lookupしたauthorityのplan / epoch / page index / identity / expected owner revision、current owner revisionを同じread guardとして再検査する。authorityが欠損または不一致ならeffect 0件で拒否し、validな場合だけdeterministic obligation direct recordのAbsentをexpectedに、authority由来の実ownerとimmutableな`ApplicationShutdownAssociation`を持つ`Pending(EffectReserved)` obligation、更新済みgenerationの独立claim record、pending inventory更新を同じcommitで確定する。commit後も共通`EffectDispatchGate(effect_id)`がI/O invocation直前にsame state / revision / claim / association / dispatch fence / unresolved scope fence、Activated lineage、page hash / ordinal、authority digestとidentity、cutoff前を再検査する。gate内ではvalidated authorityから作ったimmutable shutdown commandをcancellation-shielded owned driverへ移してstarted handleをeffect registryへnon-blocking登録するだけとし、provider response / Session / runtime-event lockを待たない。登録成功後だけscope配下へ明示commandを起動でき、登録不能はeffect 0件である。runtime effectの無いSession targetは同じguardでSession closureとcompact `Terminal(Succeeded)`を直接確定し、Pending / claim / scope fenceを作らない。global activationだけでは明示commandを起動しない。startupはlogical PreparedまたはActivated planのunreserved targetから明示commandを自動開始せず、pre-activation orphanはCancelledBeforeActivation、Activated後の未完了targetはdurable identityを保った`ReconciliationRequired`へ送る。deadline後に遅れて確定したreservationも明示commandを起動せずprovider readbackまたはexplicit safe actionで解決する。
- shutdownの13秒durability cutoffは`T0`基準のabsolute slotで閉じる。`[0, 0.25s]`はinitial root、`[0.25s, 2.0s]`はadmission settle、`[2.0s, 3.25s]`はtarget / recovery snapshot、`[3.25s, 11.75s]`はpage＋Prepared root＋activation、`[11.75s, 13.0s]`はOutcomeUnknown解決とpre-activation finalityに予約する。途中failureは残り作業を捨ててfinalityへ進み、initial initが`BeforeCommit`なら未受理のままfinalityを作らない。各Session executorは`min(executor開始 + 10s, T0 + 13s cutoff)`をdeadlineとし、`T0 + 15s`にplanと同じcanonical `ShutdownExitIntent`を持つone-shot permitでexitする。Activated後はsummary commitが不明でもsame identityとfirst intentを保つ。
- global activation前にshutdown plan / obligation storeへ書けずwriter未開始またはrollback済み`BeforeCommit`を確定できた場合だけ、process exitを行わずirreversible effect 0件でquitをabortし、admissionを再開してsafe summaryを表示する。activation commitの`OutcomeUnknown`はこのabort条件に含めず、明示command 0件のままadmissionを閉じて15秒以内にexitする。Activated後は一部明示effectが始まった可能性に加え、最終process exitのcoupled effectも確定不能になり得るためquitをabortせずadmissionも再開しない。`EffectReserved`が証明するのは明示commandだけである。ExitCoupled candidateは(a) latest-attempt / latest-activatedのplan / epochが一致し、current rootがinitial Activatedならlatest-attempt root hash、post-activation descendantならimmutable `activation_ancestor_sha256`がactivated pointer hashと一致し、かつactivated pointerのboot changeまたはsame-boot Finalizing / Stoppedがあるplan、または(b) latest-activatedがそのattemptのactivation lineageを証明せずlatest-attemptがprevious-boot Preparedを指すactivation-possible plan、の最大1件だけである。candidateの`process_exit_coupling=MayChangeOutcome` targetまたはpreexisting childはpublic stateを必ず`ReconciliationRequired`とし、`SafeEffectObservation::ExitCoupledOutcomeUnknown { plan_id, epoch }`をobservation fieldだけへ置く。`ExitCoupledOutcomeUnknown`を`ShutdownTargetPublicState`へ追加しない。final summaryが無くてもcandidate root / page / boot evidenceからunresolved targetを`ExitedWithRecovery / ReconciliationRequired`へ導出し、Completedやlatest-activated pointerを捏造しない。pending recovery queryはcandidate planのpinned snapshotへobligation keyをdirect lookupし、snapshot revision / payload hashがcurrent obligationと一致する場合だけ同じoverlayを返し、newer stateを優先する。複数plan、retiring snapshot、compacted archiveをscanしない。coordinatorは全runtimeをepoch fenceし、global deadlineの15秒以内にone-shot permitでexitする。startupはこのidentityからblind effectを開始しない。
- current shutdown projectionはTauri `get_application_shutdown` / WebSocket `GetApplicationShutdown`の同じquery service / presenterからnormal shutdown用`CurrentApplicationShutdownResult`として返す。hash-validなcanonical plan rootがfield owner、latest-attempt / latest-activated pointerはselectorと冗長cross-checkである。hash-validなcomplete rootがexactly oneでplan ID / epoch / first-ingress canonical `ShutdownExitIntent`を一意にanchorできる場合だけprojectionを構築し、そのrootとpointerの冗長semantic identityだけが矛盾する場合に限って`ShutdownAuthorityMismatch`付きReconciliationRequiredを返す。verified root不在は`Current(None)`、same-boot Accepted flightは`Current(Some(...))`、同じtransactionとplan identityへanchorできるcommit結果不明だけはwrapper `OutcomeUnknown`である。storage read / decode / envelope・self-hash / pointer-to-root hash failure、required record欠損、state composite・activation lineage integrity failure、複数rootまたはunanchorable authorityでidentityを一意にanchorできないcaseはTauriのtyped `GetApplicationShutdownApplicationError::Internal`またはWebSocketの`AgentSessionWsErrorV1::Internal`で返し、projection、`Current(None)`、embedded `OutcomeUnknown`へ隠さない。bootstrap-safe quitはnormal planを作らず`get_application_quit_operation`の`ApplicationQuitProjection::Bootstrap`から読む。fresh bootではprevious-boot nonterminalだけを同identityのReconciliationRequiredとして`Current(Some(...))`にし、previous-boot terminalはhistory queryから読む。`RetryQuit`はsame-boot pre-activation effect-0 Failed、durable fence、admission Open、store Healthyを同snapshotで満たす場合だけ返す。
- shutdown readbackはtransaction inventory、pending inventory、obligation-state-by-ID、latest-attempt / latest-activated pointer、unresolved scope-fenceを同じsnapshotへ固定し、最大4096 targetを2秒以内にfoldする。exact counts / outcomeと要求された最大128 entryだけを同じauthority revisionから作り、超過時はpartial結果を返さず`QueryBusy / DeadlineExceeded`、cursor snapshot失効は`CursorExpired`とする。`terminal_count`はcompact 4値terminal result、`completed_count`はtarget単位のterminal resultまたはderived `Superseded / CancelledBeforeActivation`を重複なく数え、`unresolved_count = target_count - completed_count`とする。current pending inventoryは`list_pending_agent_recovery / ListPendingRecovery`、planが固定したpreexisting snapshotは`get_pending_recovery_snapshot / GetPendingRecoverySnapshot`から別々に読み、別filter / requestへのcursor再利用は`CursorMismatch`、lease / retention失効は`CursorExpired`とする。両queryはsafe identity / payloadless lifecycle / safe failureを返し、exact effect payloadやraw observationを公開しない。
- target entryは`ShutdownTargetSubjectView::OpenSession { session_id, activity } | Workflow { workflow_execution_id }`を返し、public stateとは別に`terminal_result: Option<ObligationResult>`とsingle `safe_failure`、safe observationを持つ。Completedは4値result Someを必須としFailedTerminalだけfailure Some、他3値はNoneである。derived CancelledBeforeActivation / Supersededはresult / failureともNone、ReconciliationRequiredはresult Noneで保存済みreasonがある場合だけfailure Someとする。target-state hashはcanonical subject hash、authority availability digest、public state、result Option/value、safe observation Option/hash、safe failure Option/hashを含み、authorityまたはstateの変更で旧action guardを必ず失効させる。
- pinned snapshot refはcount 0を含む`ClosedSession / ArchivedSession / UnownedRuntime` exactly 3 rangeを常に持つため、3つのrequest partitionは全てvalidでempty rangeは空pageを返す。unknown wire tag / limit範囲外はInvalidRequest、plan / epochまたはsnapshot refとrootのbyte不一致だけはSnapshotMismatch、cursorを別plan / snapshot / partitionへ再利用した場合はCursorMismatch、lease失効はCursorExpired、compaction後はDetailsCompactedであり、別authorityへのfallbackで隠さない。
- action IDはbackendが発行するopaque identityである。frontendはparse / 生成せずprojection値をcommandへそのまま返す。Tauri / WebSocket共通の`resolve_pending_recovery_action`と`resolve_shutdown_target_action`は、current resource / plan detailsより先にdurable action decisionをidentityでlookupする。
- resolverはclosed `RecoveryActionCommandResult = Completed { result } | InProgress { action_id } | Rejected { rejection } | ActionOutcomeUnknown { action_id }`を返す。rejectionは`NotFound | RevisionConflict { current_revision } | ActionUnavailable | TargetRevisionChanged`だけである。same action IDのCompleted decisionは保存済みreceipt、result classification、canonical safe resultをexact replayし、current resource revisionの前進やplan detail compactionを理由に再実行しない。nonterminal decisionはsame attemptへjoinしてInProgress、writer開始後のcommit結果不明だけはActionOutcomeUnknownとし、WS / Tauri errorへ複製しない。decision Absentのfresh actionだけがcurrent revision / root / state guardを検証する。ReadAgain / RetrySameEffectはclaimとreservationをwrite-aheadしてからexternal I/Oへ進み、UseObservedResult / CancelIfSafe / KeepForManualResolutionはkind-specific side stateとCompleted receiptを同じclosureで確定する。same action IDのresponse lossをRevisionConflictへ変換せず、異なるstale actionだけをeffect 0件のRevisionConflict / ActionUnavailable / TargetRevisionChangedとする。
- resource-isolated send input、operation / resource privacy purge、managed backup / restore、app-data reset、complete privacy authority reset / importは#1499のPhase 0 runtime contractに含めない。必要なdata lifecycleと物理reclamationはD3 / F3の後続設計で、独立した公開要件とmigration契約を先に確定する。
- action resolverはgeneric result-only terminal pathを持たない。ReadAgainはadapter resultのproof ref / pending revision / safe observationを同closureで確定し、UseObservedResult / CancelIfSafeもcurrent obligation revisionへ束縛したproofだけを使う。開始 / ackだけのproofではpending obligation＋proof ref / safe observation＋owner / operation status＋action attempt Completed receipt / safe resource result（outcome=Pending）を同じclosureで更新し、Outcome Pendingを返す。terminal proofがある場合だけ、TurnExecution / QueueExecution / TerminalCommit→R-020 canonical terminal closure、ProviderEstablish→dependency / send operation status、PermissionDelivery→permission settlement、BackendRecovery→RecoveryPublication Pending handoff、RecoveryPublication→message publication、SessionClose→Session / configuration / archive projection、WorkflowShutdown→workflow stateのkind-specific closureへ委譲する。compact result、claim / pending index / scope fence削除、proof由来safe observation digest、side state、action attempt Completed receipt / safe resultは同じcommitで、required side participant failure時はresultを先行させず元pending identityを保つ。QueueExecutionのCancelIfSafeは#1404へ委ねる。
- shutdown target actionはcurrent plan / detailsより先にaction decisionをlookupし、Completedなら保存済みresultをexact replayする。decision Absentまたはnonterminal attemptの継続時だけlatest current planをplan / epoch / target key / expected revision / root / target-state guardで検証する。Succeeded / ConfirmedNoEffectはSessionClose / WorkflowShutdownのowner projection、compact target result、action receipt、必要なclaim / index / fence cleanupを同じclosureで確定する。Ambiguousは同じtargetをPending(ReconciliationRequired)に保ちつつ、readback action自体のCompleted receiptとsafe resultをsame action IDへexact replayする。idempotent explicit retryはEffectReserved、claim、pending index、scope fence、action attemptを先に確定し、`EffectDispatchGate`のcall直前検査後だけ同じeffectを実行する。最後のtargetではtarget result、plan Completed、latest-attempt pointer、summaryを同じclosureで確定し、partial completionを作らない。
- global activation前の`Failed(epoch=n)`後、same-boot、external effect 0件、durable Failed fence、global admission Open、store Healthyを同snapshotで確認し、専用`ApplicationShutdownAction::RetryQuit`をユーザーが選んだ場合だけnew plan・`epoch=n+1`の新しいflightで再試行する。Cancelled、activation outcome unknown、process-only failure、Stalled、fresh bootまたはactivation後はこのactionを返さない。process flightが無い状態でsame-bootまたはprevious-bootのnonterminal planがcurrent projectionである間はnew epochを発行せず同identityのresolutionを要求する。previous-bootのeligible terminal planはこの拒否へ含めず、same-boot terminalと同じnew-flight / compaction guardへ進める。activation outcome unknownまたはactivation後は再試行画面へ戻らず15秒以内にexitする。全completionはshutdown associationのplan ID / epochとobligation claim generation / tokenをCASし、deadline後に戻った旧処理が新しいSession、Workflow、shutdown resultを変更したり重複exitしたりしない。pre-activation Failed / Cancelledのterminal closureはeffect 0件の`AbortedBeforeActivation(details=Available)`を保存し、full page / target authority / snapshot refを保持する。same-boot current flight joinとprior nonterminal / unresolved scope-fenceの`PreviousShutdownReconciliationRequired` state guardを先に通過した後、new flightのroot-init時にsame-bootまたはprevious-bootのeligible terminal prior latest planがdetails Availableなら、retiring pointer Noneをguardして旧latestをat most oneのretiring full-detail planへ移し、new minimal Preparing root / latest-attempt pointer / retiring pointerを同じclosureで確定する。retiringが既にSomeならexact `PreviousShutdownCompactionPending`でnew plan identity / root / page / effect 0件のBeforeCommit rejectionにし、priorがCompactedならarchive parityを確認してretiringを変更しない。complete Prepared publishはretiring pointerを変更せず、compactionをroot-init / Prepared / activation commit pathへ入れない。guarded background compactorはretiring planのroot / pointer / obligation-state snapshotをfoldし、`ArchiveSwitch → DetailDetachChunk → FinalizeDetach`をdurableに進める。ArchiveSwitchはarchiveと、original page suffix・authority ordinal / prefix・pending materializationをcleanup-only authorityとして持つcompact rootを確定し、detail delete 0のままqueryをarchive-onlyへ切り替える。Phase 0 DetailDetachChunkは最大64 records / decoded 4 MiB / 50 msのordered batch overlayとcompact-root cursorを先にcommitし、post-commit materializerのdirect absenceを完了証拠にする。F3は`InventoryDetachChunk`のrow / item stateで再開する。Phase 0 FinalizeDetachは最大4 pages / 4 MiB / 50 msのpage overlayをcommit後materializeし、全page absence後の0-page dedicated closureだけpointerをclearする。F3 SQLはlast atomic batchまたはpage 0 transactionでclearする。各logical commit / materialization / clear前後のcrashはarchive、compact-root cursor / residual suffix / pending batch、pointerから再開する。retiring plan未finalize中にprior latestもdetails Availableなら次のroot-initを上記exact errorで拒否するため、full detail setはlatest plan（Preparing / Preparedを含む）＋retiringの最大2件である。
- Tauri `RunEvent::ExitRequested`はCmd-Q、Dock、OS logout / shutdownのoriginを識別できないため、productionでは`NativeExitRequested`として扱う。識別不能なoriginをlog / event / enumへ捏造しない。eventが届かないhard killはI2のCrash recoveryが担当する。

## turn 状態機械（明示化）

ST-3 の解消として、phase 遷移を規範として一覧化する（実装は本表と 1:1 対応のモジュールに分解する）:

| phase | 入力イベント | 遷移先 | 必須アクション |
|---|---|---|---|
| Idle | start_turn（独立activation済み、configuration / Goal ready） | Streaming | policy/gate再検証後、`TurnStarted`にprovider/model/mode/effective effort/protocol identity/goal refを記録し、provider I/Oへ進む |
| Idle | start_turn（turn/startでactivation） | StartingTurn | human message＋`TurnStartRequested`をatomic append。queue/Bypass intentも同batch。early provider eventはbuffer |
| StartingTurn | provider ack/readback＋canonical batch成功 | Streaming | Session pending activationなら`SessionConfigurationActivated + TurnStarted`、queue per-turn overrideなら`QueueItemStarted + TurnStarted`をatomic commitしbuffer公開。queue値でSession effectiveを変更しない |
| StartingTurn | timeout / provider ack後commit失敗 | Idle | provider turn/config observation付き`TurnStartReconciliationRequired`。interrupt/readbackし、send/config/Goal/queueをblock |
| StartingTurn | interrupt 要求 | Interrupting | 起点に関係なく`TurnInterruptRequested + QueuePaused`をdurable化。late ackは通常commitせずinterrupt/finalize、結果不明はTurnStart reconciliation |
| Idle | start_turn（NeedsResolution / ReconciliationRequired / ProtocolIncompatible） | Idle | provider I/O を行わず理由とRustが算出した解決操作を返す（I14/I15） |
| Idle | Fatal | Idle | Notice(Error) durable 化（I12、RT-6） |
| Streaming | PartsMerged / TokenUsageUpdated | Streaming | merge → 定期 flush（I3） |
| Streaming | PermissionRequested | WaitingPermission | permission part durable 化＋state change emit |
| Streaming | 通常`TurnCompleted(Completed)` | Idle | finalize（Final化→TurnResult記録→workflow通知）。queueが既にunpausedで再検証成功時だけ次itemを1件開始 |
| Streaming | Failed / Fatal / Timeout / Crash / ProtocolIncompatible terminal | IdleまたはReconciliationRequired | finalizeし、QueuePausedを確定または既存pauseを維持。自動drainしない |
| Streaming | interrupt 要求 | Interrupting | backend interrupt 送出 or 予約（I5） |
| Streaming | Fatal / stream 終了 | Idle | Interrupted{Crash} で finalize（I1） |
| WaitingPermission(Pending) | respond_permission | WaitingPermission(Responding) | exact validated payloadをowner-only private blobへsyncし、redacted intent＋private ref＋`PermissionDelivery` obligationをdurable化してからbackend送出 |
| WaitingPermission(Responding) | response ack | Streaming | `PermissionResolved`をappendしeffectiveを確定 |
| WaitingPermission(Responding) | provider explicit reject（requestはPending） | WaitingPermission(Pending) | `PermissionResponseRejected`で旧responseを終端し、private payload refを解放して新responseで再回答可能にする |
| WaitingPermission(Responding) | timeout / restart recovery | WaitingPermission | cancel/tool-start/readbackを相関。確定不能はpermission reconciliationで再回答をblock |
| WaitingPermission | permission cancel（CLI 取り下げ） | Streaming | Cancelled(effective=false) へ更新（I7） |
| WaitingPermission | interrupt 要求 | Interrupting | pending permission を Cancelled(effective=false) へ畳む（I7）＋backend interrupt 送出 or 予約（I5） |
| WaitingPermission | TurnCompleted | Idle | 未解決 permission を Cancelled に畳んで finalize（現行踏襲） |
| Interrupting | TurnCompleted / 猶予超過 | Idle | finalize。猶予超過時は強制終端＋Interrupted{Timeout} |
| non-Idle | accepted normal Session close / open-session archive | IdleまたはReconciliationRequired | Final parts＋permission settlement＋QueuePaused＋SessionClosed terminal＋Closed / Archivedを初期manifestへ入れる。runtime scopeありはSessionClose(Pending)を同時予約しEffectReserved後にclose、観測closureでTerminal化する。scopeなしはcompact Terminal(Succeeded)を初期manifestへ直接入れる。不明なら確定済みstateを保ってreconcile（I1/I17） |
| Idle | accepted normal Session close / open-session archive | IdleまたはReconciliationRequired | synthetic terminalなしでQueuePaused＋Closed / Archivedを初期manifestへ入れる。runtime scopeありはSessionClose(Pending)を同時予約しEffectReserved後にclose、観測closureでTerminal化する。scopeなしはcompact Terminal(Succeeded)を初期manifestへ直接入れる。不明でもreopenしない（I1/I17） |
| （全phase） | graceful quit target execution | IdleまたはReconciliationRequired | 全target prepare→global activation後だけper-target EffectReserved→Session / Workflow closure。active SessionはSessionClosed terminal、Idle Sessionはsynthetic terminalなし（I17） |
| non-Idle | backend switch | 同一phase | typed Busy。永続状態、provider、runtime、queueを変更しない（I1/I17） |
| Idleかつpending permission / recovery / obligationなし | backend switch | IdleまたはReconciliationRequired | old runtime scopeありは初期D1 closureでold effective guard＋QueuePaused＋SessionClose(Pending)予約を確定し、EffectReserved後にclose、観測closureでTerminal化した後だけnew effectiveへ進む。scopeなしは初期closureでcompact Terminal(Succeeded)＋new effectiveを直接確定する。既存itemは保持し不整合ならNeedsResolution、不明ならold effective / pause維持（I14/I17） |
| （全phase） | chat / workflow / workspace / window view close | 同一phase | turn、permission、queue、runtimeへ作用しない（I17） |
| （全phase） | hard kill | restart後IdleまたはReconciliationRequired | dangling turnをCrashで一度だけ回収しqueueをRecoveryHoldへ置く（I2/I17） |
| Idle | configuration update request | Idle | CAS / capability / policy / challenge検証→durable intent→adapter送信（I14）。非IdleはBusy |
| Idle | configuration ack success | Idle | canonical eventでselectedをcommit。live以外はawaiting化（I14） |
| Idle | configuration activated | Idle | canonical event後にeffectiveを進めて`Synced`化（I14/I16） |
| Idle | configuration reject | Idle | rejectionを記録し旧selected/effectiveを維持（I14） |
| Idle | Goal user transition request | Idle | goal CAS→durable intent→strategy/effects検証→adapter送信（I15） |
| （全 phase） | provider/evaluator Goal observation | 同一 phase | adaptor/gatewayが`observation_id`付きraw evidenceを先に保存し、domainへnormalized snapshot＋evidence refを渡してgoal id/revision照合→遷移検証→evidence付きcommit。対応transitionが無い競合は独立`reconciliation_id`で回復（I15） |
| （全 phase） | timeout / partial apply / canonical commit failure / conflict | 同一 phase | 対象aggregateを`ReconciliationRequired`。新規turnをblockしreadback/reapply/rollback等（I14/I15） |
| Idle | control-plane protocol drift | Idle | `ProtocolIncompatible`をdurable化し、新規turnをfail-closedでblock（I10/I14） |
| Streaming / WaitingPermission / Interrupting | control-plane protocol drift | Interrupting → Idle | `ProtocolIncompatible`をdurable化し、pending permissionをCancelled、provider interrupt、猶予後`Interrupted{ProtocolIncompatible}`で必ずfinalize。以後send block（I1/I5/I7/I10） |

queue はphaseと直交するevent-sourced sub-state: `items: [{ input_ref, configuration_snapshot, goal_ref, status: Queued/AwaitingBypassConfirmation/Starting/Started/Failed/Cancelled/NeedsResolution }]`＋`paused: bool`（I4/I5）。configurationとGoalも別aggregateとしてselected/effective/pending/sync state、current/pending/sync stateをread modelから観測できる。

## シナリオ別保証（受け入れ基準の骨子）

| シナリオ | 保証 |
|---|---|
| 正常完了 | TurnResult(Completed{stop_reason}) が durable。reload 後も同一表示（P1） |
| ユーザー Stop | provider / lock hangかつstorage正常ならAcceptedから最悪10sでInterrupted{Timeout}。storage failure時はpre-reserved obligationを表示して通常Idleへ進まない。queueはpaused、入力無損失 |
| streaming中にnormal Session close / open archive / graceful quit | Final化済み本文、Interrupted{SessionClosed}、permission残骸なし、queue paused・無損失（I1/I17） |
| streaming中にchat / workflow / workspace / window view close | turn、permission、queue、runtimeが継続し、再表示時に同じ状態（I17） |
| streaming中にbackend switch | Busyで拒否され、backend selection、terminal、runtime、queueは不変（I1/I17） |
| hard kill → restart | flush済み本文を復元し、dangling turnはInterrupted{Crash}、queueはRecoveryHold（I2/I17） |
| クラッシュ → 再起動 | dangling turn が Crash で回収済み。損失は最大 1s の本文のみ（I2/I3） |
| recovery中sessionをfork | typed reasonで拒否。stable forkでも親固有のpending recovery/delivery obligationを継承しない（I2/I9） |
| resume 失敗 | 両 backend とも新規 establish ＋ Notice。以後のターンは正常。editor_context 保全（I9） |
| active-turn steer | inputとTurnSteerRequestedをprovider I/O前にcommit。Accepted/Rejected/ResultUnknownを区別し、結果不明を暗黙再送しない（I6） |
| queue に 2 件積んで再起動 | queue が復元され、明示操作で実行再開できる（I4） |
| queueありturnが通常完了 | queueがunpausedかつ再検証成功時だけ次itemを1件開始。Stop / close / archive / quit / failure / crashでは開始しない（I4） |
| permission 待ち中に interrupt | ダイアログは即 Cancelled 表示。誤記録なし（I7） |
| permission回答の送信中にcrash | redacted intentと`PermissionDelivery`から同じresponseを発見し、owner-only private refでexact payloadを保持する。readback / reconciliationを行いblind resendしない（I8） |
| 5 mode の切替成功 | provider ackとcanonical selected/activated event後だけrevisionと表示が更新され、次turn/reload/resumeでも同じmode（I14） |
| selected commit前のprovider reject | UIは旧selected/effectiveのまま、非対応理由またはNotice(Error)が出る（I8/I14/I16） |
| selected commit後のactivation reject | new selected / old effectiveを保ってReconciliationRequired。rollbackは新revisionとして記録しCASを逆行させない（I14/I16） |
| provider ack 後に canonical event append 失敗 | old stateへ戻ったふりをせずReconciliationRequired。未完了requestからrestart後もreadback/reapply/rollbackを継続（I14/I15） |
| canonical event 後に SessionMeta cache 更新失敗 | provider driftとは扱わずPersistFailureを表示し、event logから再投影する（I14） |
| next-turn / restart effort 更新 | selected と effective を別表示し、activation 後だけ effective revision が進む（I14/I16） |
| stale base revision から並行更新 | provider へ送信せず conflict。snapshot 再取得後に再操作できる（I14） |
| model 変更で推論レベル非対応 | silent fallback せず、利用可能な推論レベルと解決操作を表示する（I16） |
| Goal を設定→pause→restart→resume | Goal専用pending/sync stateとid/objective/status/revisionを保持し、strategy/effectsを表示して再開する（I15） |
| Claude `/goal` でGoal設定/編集 | durable Goal intent後にCLI commandを実行。StartsTurn等のeffectsを表示し、Goal canonical event＋TurnStartedのatomic batch commit後にだけ公開（I15） |
| Goal 完了後に古い queue item を drain | goal id / revision 不一致を検出し、Goal を復活させず rebase / 取消を要求する（I4/I15） |
| Bypass 選択 | execution固有guardへ束縛し、managed policy再検査後にconsume＋durable intentをlocal atomic batch commitしてからproviderへ送る。失敗後も同一intentだけ再試行可（I14） |
| New Agent create応答が喪失 | correlation/idempotency keyとstageからreadback/reuse/cleanup。存在不明かつ安全なlookup/createが無ければRecreate不可（I14） |
| initial Goal適用中にcrash | local SessionとGoal transitionの相関を復元し、同じtransitionをreconcileしてGoal/turnを重複起動しない（I14/I15） |
| compiled schemaと実行CLIが不一致 | Session前はlaunch attempt、Session後はAgentProtocolStateにProtocolIncompatibleを表示し新規turnを開始しない。部分identityとraw control refを監査可能（I14） |
| Auto / Bypass で workflow checkpoint 到達 | mode に関係なく workflow は停止し、人間の承認・却下・再指示を待つ（I15） |
| stdout に識別済みdiagnostic非JSON行 | 両backendともskip件数をNotice化して継続。未分類/malformed/oversize frameはProtocolIncompatibleでsendをblock（I10） |

## backend 差の吸収規約

| 関心事 | 保証水準 | Claude | Codex |
|---|---|---|---|
| interrupt | 常に受理・最悪 10s で終端（I5） | synthetic abort 踏襲 | turn_id 予約＋猶予強制終端を追加 |
| 生存シグナル | 長考中もシグナル継続（I11） | thinking delta ＋ keep_alive | reasoning delta 購読を追加（CX-3） |
| stdout 頑健性 | 識別済みdiagnosticだけskip、content-plane未知はNotice、未分類/malformed/oversizeはfail-closed（I10） | 既存を分類付き可視化へ強化 | 共通ラッパ導入でprocess即死を閲覧可能なProtocolIncompatibleへ置換 |
| steer | 非対応は queue へフォールバック（I6） | queue | queue（将来 turn/steer 対応時に置換） |
| resume 失敗 | Cleared → 再 establish ＋ Notice（I9） | 無言復旧を通知付きに | 恒久死を回復経路に接続 |
| AgentMode | schema supportとruntime availabilityを分け、近似・非対応を明示（I14） | `default / acceptEdits / plan / auto / bypassPermissions`。auto availabilityとBypass launch opt-inを実行時検証 | approval / sandbox / reviewerとruntime取得した`collaborationMode/list` presetを構成 |
| Auto review | provider reviewerの境界を広げず、全statusを監査 | classifier active / manual fallbackを`AutoOperationalState`へ | approved/deniedだけAuto解決。inProgress/timedOut/abortedはactivity/未解決へ |
| ReasoningEffort | selected/effective/unknown、source、option/default/反映時点を返す（I16） | runtime API/readbackが無ければpinしたversion×model table。検証不能はUnsupported | `model/list`の広告順・defaultを保持しturn overrideへ写像 |
| Goal | current Goal最大1件、独立id/revision/pending/sync、操作別strategy/effects（I15） | `/goal`はProviderCliCommand。setはStartsTurn、pause/resume emulationは進捗reset等を明示 | typed RPC、notification、全statusとread-only accountingを`ProviderGoalSnapshot`へ |
| protocol identity | generated schemaと実行binary/flags/capabilitiesの一致を必須化（I14） | CLI/SDK versionとlaunch gatesを照合 | executable version、schema hash、experimental flags、initialize capabilitiesを照合 |

## トレーサビリティ（本書が解消する問題）

| 問題 ID | 不変条件 / 節 |
|---|---|
| RT-1 | I1 |
| RT-2 | I2, I3 |
| RT-3, OB-3 | I4 |
| OB-4 | I4（cancelled マーク） |
| OB-5 | I5（queue paused） |
| OB-6, RT-7 | I4（起動失敗の可視化） |
| OB-1, SD-2 | I5 |
| OB-2 | I6 |
| OB-8 | I9 |
| CL-1 | I7 |
| CL-2, CX-6 | I8（ack 駆動） |
| ST-4, RT-4, RT-8 | I8（失敗の着地・自己修復） |
| SD-1 | I9 |
| SD-3 | I10 |
| SD-4, ST-9 | I11 |
| RT-6, RT-5 | I12 |
| FE-1, FE-2, FE-3（backend 側の裏付け） | I7, I12, I3 |
| CX-2 | I7 の変種: elicitation は「応答義務のある要求」として permission 経路に載せる（無応答ハングの解消。語彙は V-D6 の Question を流用） |
| ST-3 | turn 状態機械の明示化 |
| ST-5 | I13 |
| #1445, #1446 | I14（正本・ack・revision・永続化・migration） |
| #1447 | I14（5 mode cross-backend mapping） |
| #1448 | I16（ReasoningEffort capability / 反映時点） |
| #1449 | I15（Goal lifecycle / provider capability） |
| #1450 | I4, I9, I14, I15, I16（workflow / queue / restart 継承） |
| #1451 | I14, I15, I16（backend-owned projection / available actions / capability-driven UI の前提） |
| #1491 | I2 / I4（bounded recovery/status/current-turn query） |
| #1385, #1494 | L-P6（transaction / schema / history-independent commit） |
| #1497 | I4（queue pause bounded projection） |
| #1498 | I6（steer write-ahead / outcome / reconciliation） |
| #1499 R-001〜R-013 | I1, I2, I4, I5, I8、D3 physical design gate |
| #1499 R-014 | I1, I4, I7, I17、close / quit decision table |
| #1499 R-015〜R-016 | I17、L-D14 |
| #1499 R-017 | I2、I4、L-D1、L-D3のbounded discovery / direct identity |
| #1499 R-018 | I2、I4、I8、I17のTauri / WebSocket共通query / presenter |
| #1499 R-020 | I1、I4、I5のcanonical terminal closure / Stop競合 |
| #1499 R-021 | I2、I17のrecovery action / shutdown target action / kind-specific closure |

## 設計判断

- **L-D1（2026-07-15改訂）**: queueの正本はSession event logとする。enqueue/cancel/execution/started/failedをappend-only eventで記録し、session queue stateとmessage cancelled markerは再構築可能なprojectionにする。通常UIはactive/nonterminal projectionだけを保持し、terminal履歴はpage参照してfull-retentionを避ける。旧「session stateへ上書き、event replay外」の判断はcrash時atomicityを満たさないためsupersedeする。
- **L-D2（2026-07-19改訂）**: request ingressでslotを先取りしたsame-boot monotonic T0をStopAcceptance commit成功時だけAccepted authorityにし、対応するUTC `accepted_at`と監査用`acceptance_committed_at`を別記録する。absolute deadlineはmonotonic `T0 + 10s`で、response遅延やwall clockを理由に後ろへ動かさない。restartではpersisted時刻を監査 / expired分類にだけ使い、期限切れまたは残時間不明をR-009へ送りaccepted_atを再採番しない。distinct未完了target最大32件、same-target join、serialized occupancy / schedule 4 MiB以下とする。StopAcceptance / terminal critical manifestへ各125 msを予約し、32 targetsの2 commitで8s、残りをschedule余白とする。acceptance manifest自身がTerminalCommitの更新済みclaim generation / EffectReservedと独立claim recordまで確定し、後続reserve commitを置かない。`T0 + 1.5s ... T0 + 9.25s`へforce slotを割り当て、`T0 + 9.5s`までにterminal commit、残り0.5秒でquery / emitする。schedule不能な33件目はpre-provider `StopCapacityExceeded`とする。10秒でReconciliationRequiredになってもunresolved occupancyはterminal result確定まで32件へ数え続ける。storage failure時はpre-reserved obligationを維持し、通常Idleへ進めない。
- **L-D3（2026-07-19改訂）**: crash / recovery回収は3-tree pending-only obligation inventoryのcanonical primaryをstartupでbounded page列挙する。primaryだけが各pending obligationを1件countし、actual owner / shutdown association secondaryを重複列挙しない。Pending / Terminal canonical stateは別のobligation-state-by-ID COW rootでbounded lookupし、両rootを`Phase0ReadSnapshotRef`で同じrevisionへpinする。All / Partition、Owner、ShutdownPlan queryはそれぞれprimary、owner secondary、association secondaryを使い、全Session / event / partition走査や開いたSessionを待つlazy discoveryへfallbackしない。transitionはdirect obligation、state root、pending中の3 tree refを同manifestで更新し、transaction-inventory commit前にlogical rootを前進させない。回収通知自体はI2の表示規則どおり静かに行う。
- **L-D4**: queue 取り消しは human message の削除ではなく cancelled マーク。履歴の誠実性と OB-4（復元コンテキスト除外）の両立。
- **L-D5**: interrupt 後の queue は paused（自動 drain 禁止）。「止めたのに続く」（OB-5）の解消を優先し、再開は明示操作。
- **L-D6**: Agent configurationはdiscriminated patch、write-ahead intent、canonical event commitを使い、selected/effective/pending/reconciliationを分離する。event logが正本、SessionMetaは再構築可能なprojection/cacheであり、外部providerとのatomic commitを仮定しない。
- **L-D7**: GoalはSessionごとにcurrent最大1件、configurationとは独立したgoal id/revision/pending/sync stateを持つ。workflow nodeは開始時にinitial Goal specを解決し、新規Goalとして記録する。
- **L-D8**: Reasoning effortのselected/effective/unknown、値・説明・default・反映時点はprovider APIまたはprotocol identityにpinしたcompatibility table駆動。Releash固有の固定enumやTokenUsageからの推測は持たない。
- **L-D9**: workflow definitionはrequired/optional overrideを分けた`AgentConfigurationTemplate`、WorkflowExecution/NodeExecutionはprovenance付き`ResolvedLaunchConfiguration`、provider ack後のSessionは`AgentSessionConfigurationProjection`と型を分ける。
- **L-D10**: queue item は enqueue 時の effective configuration snapshot と Goal ref を保持し、後続の Session 設定変更では書き換えない。変更は明示 rebase のみ。
- **L-D11**: closeとquitの意味論はclose / quit decision tableを唯一の正本とし、view close、Session lifecycle、configuration transition、application quitを別のtyped intentとして扱う。
- **L-D12**: backend switchはIdle限定でactive turnを終了しない。旧I1 / #1405のbackend-switch SessionClosed契約は#1499によりsupersedeする。
- **L-D13**: native `ExitRequested`のoriginは推測しない。graceful eventが届かない終了はCrash recoveryとして扱う。
- **L-D14**: graceful quitはinitial Preparing root、complete Prepared root、以後の全transitionを追う`LatestShutdownAttemptRefV1`、およびrootのglobal activationと同commitの`LatestActivatedShutdownPlanRefV1`更新を分離する。activationはlatest-attempt pointerのsame plan / epoch / Prepared root hash / revisionをguardとし、commit確認後もcurrent coordinatorだけがplan / epoch / target revisionを再検査し、per-target `EffectReserved`を確定する。明示shutdown commandはさらに`EffectDispatchGate(effect_id)`がstate / terminal Absence / claim boot・lease / effect・intent hash / association / dispatch fence / scope fence / action attempt generationをinvocation直前に再検査する。gate内handoffはnon-async / non-blockingで、immutable commandをcancellation-shielded owned driverへ移しstarted handleをregistryへtry-registerするだけとし、provider response / Session / runtime-event lockを待たない。登録不能はeffect 0件、terminal / reclaimは同じgate＋registryで先勝判定し、result waitはgate解放後の残予算だけで行う。startupはPrepared / Activatedのunreserved targetから明示commandを開始せず、late EffectReservedもblind実行しない。13秒durability cutoffは`T0`基準のabsolute slotで閉じる。`[0, 0.25s]`はinitial `ShutdownRootInitialized` 1 closure、`[0.25s, 2.0s]`は残りadmission settle 1.75秒、`[2.0s, 3.25s]`はtarget / recovery snapshot enumeration / encode 1.25秒、`[3.25s, 11.75s]`は最大32 page＋Prepared root＋activationの34 closuresを各250 msで8.5秒、`[11.75s, 12.5s]`はOutcomeUnknown lookup / scheduling 0.75秒、`[12.5s, 12.75s]`はactivation未成立時だけdurable Failed / Cancelled finality 1 closure、`[12.75s, 13.0s]`は0.25秒marginである。途中failureは残りpage slotを捨ててfinality slotへ進み、initial initが`BeforeCommit`なら未受理のままfinalityを作らない。Session deadlineは`min(executor開始 + 10s, cutoff)`、global exitは15秒とする。全target準備またはactivation writer開始前のfailure / rollback済みBeforeCommitならprocess exitせずeffect 0件でquitをabortしadmissionを再開する。activation OutcomeUnknownはabort / admission reopenせず明示command 0件のまま15秒以内にexitし、fresh bootが同identityをresolveする。activation後もabort / admission reopenを禁止し、terminal / completion / EffectReserved未到達targetをprepared identityで残す。このexitによるpipe close / job object / parent-deathの暗黙作用は0と推測せず、未完了targetとpreexisting childを`ExitCoupledOutcomeUnknown`としてrestart readback / reconciliationする。全target terminal時はplan stateを必ずCompletedにし、non-success / preexisting recoveryはsummary outcome ExitedWithRecoveryへ表す。readbackは`Phase0ShutdownReadSnapshotRefV1`の同一leaseで2 pointer / scope fenceと最大32 page / 4096 targetを2秒以内にexact foldし、current pending一覧とexact pinned snapshot queryを別authorityにする。pre-activation Failed / Cancelledはeffect 0件の`AbortedBeforeActivation(details=Available)`としてfull refs / snapshotを保持し、同じterminal closureでcompactしない。same-boot current flight joinとprior nonterminal / unresolved scope-fenceのexact `PreviousShutdownReconciliationRequired` state guardを先に行い、eligible terminal prior latestがAvailableかつretiring Noneの場合だけnew root-initでretiringへ移し、new Preparing / latest pointerと同一closureで確定する。retiringがSomeならexact `PreviousShutdownCompactionPending`、priorがCompactedならarchive parityを検証してretiringを変更しない。root-init / Prepared / activation commit path外のguarded background compactorは`ArchiveSwitch → DetailDetachChunk → FinalizeDetach`をdurableに進める。ArchiveSwitchはarchiveとPhase 0 cleanup cursorを持つcompact rootを確定しdetail delete 0のままqueryをarchive-onlyへ切り替える。Phase 0 detail / page cleanupはordered batch overlayとroot ordinal / prefix / residual suffix / pending transactionを先にcommitし、post-commit materializerのpartial deleteを同batchから再開する。全page absence後の0-page dedicated closureだけretiring pointerをclearする。future F3 SQLはInventoryDetachChunkのrow stateとlast atomic page batch / zero-page clearを使う。full detail setはlatest＋retiringの最大2件に閉じる。部分prepared dataとpre-activation abort後のlate commitはinert、same-boot pre-activation Failed後だけnew plan / epochで再試行する。
- **L-D15**: Phase 0 bridge初回upgradeはexclusive writer lockとnormal mutation / effect admission閉鎖の下でautomatic bootstrapする。immutable legacy inventoryをread最大200 source logical records / decoded source 16 MiB、別physical K tier最大17 MiB、normal lane 64 MiBでcrash-resumableにstagingへnormalizationする。一sourceはdeterministic substep microcommitへ分割できるがcursor / imported countはfinalize後だけ進め、partialをpublicへ出さない。terminal / permission / unknown raw bytesを保持し、証明不能active turn / queueをReconciliationRequiredへ、局所破損を最小scope quarantineへ写す。quarantine exact raw bytesはprivate immutable blobへ1 MiB chunk / step 16 MiBでcrash-resumableに保存し、final size / hash検証後だけrecordをcommitしてordered raw hashをparityへ含める。legacy sourceのparity検証、Phase 0 activation、F3 one-shot importに必要なretentionが全て完了するまで保持し、Phase 0ではreset / purgeをGC triggerとして扱わない。source / staged parityとrequired sync後のauthority pointer CASだけがcutoverであり、stagingはそれ以前non-authority、切替後はlegacy read-only、dual write / fallback / rollbackなしとする。Tauri / WebSocketは同じread-only bootstrap projectionを返し、pointer / root / parity検証、reachable replay / pending inventory validation、normal admission open後だけNoneにする。bootstrap中のquitはnormal plan / target /明示commandを作らない13秒settle・15秒one-shot bounded exitで、OutcomeUnknownとimplicit process-exitをauthority resultへ推測変換しない。physical identity helperはu16 BE count / u32 BE length / u64 BE unsigned numericを含む最大16 component、各1024 bytes、canonical input 16 KiB、logical key 1024 bytesをhard limitにし、signed numeric / decimal ASCIIを禁止する。

L-D15の`LegacyBootstrapCursorV1`は`source_entry_ordinal / source_entry_id / source_record_ordinal / substep_ordinal`を持つ次未commit unitのcursorである。ordinalは0..=9223372036854775807、source entry IDはraw UTF-8 1..=1024 bytesとし、enclosing source inventory hashとordinal位置のstable IDをbyte検証する。source finalize後だけ次record / entryへ進め、partial substepではsubstepだけを進める。Noneはfixed inventoryが空または全source finalize済みの場合だけである。bootstrap-safe quitはcaller binding、opaque backend operation ID、bootstrap flight locator / recordを保存し、operation queryをnormal shutdown plan lookupへfallbackさせない。

## 確定事項（2026-07-07、2026-07-15 レビューで確定）

1. **I5 / L-D5**: interrupt 時は queue を**常に paused** にする（選択式は不採用）。再開は queue chips の明示操作。
2. **L-D4**: 取り消した queue メッセージは **cancelled マークで transcript に残す**（非表示は不採用）。復元コンテキストからの除外は共通実施。
3. **I2**: crash 回収は**中断チップのみ**（起動時のバナー・ダイアログ通知は出さない）。pending-only obligation inventory のbounded pageで発見した未完了回収も、対象の正規な投影先だけに静かに表示する。
4. **I14 / L-D6**: provider/model/mode/reasoning effortはRust-owned configuration、Goalは別aggregateとする。canonical eventをcommit pointにし、provider ack後のappend failureを旧値維持とは扱わない。
5. **I15 / L-D7**: GoalはSessionごとにcurrent最大1件、独立goal id/revision/pending/sync stateとする。restart/resume/workflow継続で保持し、strategy/scope/effects付きNative/Emulated/Unsupportedを明示する。暗黙prompt fallbackは行わない。
6. **I16 / L-D8**: 工数はmodelの応答・推論強度を調整するbehavioral signalとし、selected/effective/unknownとcapability source/反映時点に従う。TokenUsageや各種budget、厳密な上限は範囲外。
7. **Auto / Bypass**: Auto の判定は provider classifier / reviewer が行い、Releash は結果を監査する。Bypass は Rust 側の managed-policy 検査と二段階確認を必須とする。どちらも workflow checkpoint を迂回しない。
8. **L-D9 / L-D10**: workflow template、解決済み launch config、Session projection、queue snapshot を別型にし、暗黙 inheritance / fallback / mutation を行わない。
9. **protocol identity**: generated schemaとspawnしたCLI/flags/capabilitiesをinitialize時に照合し、control-plane driftはProtocolIncompatibleとしてfail-closedにする。
10. **I17 / L-D11〜L-D14**: view close、Session close、backend switch、application quitを別intentとし、backend switchはIdle限定、graceful quitのSession deadlineは`min(executor開始 + 10s, 13s cutoff)`、global exitは15s、native exit originは推測しない。
