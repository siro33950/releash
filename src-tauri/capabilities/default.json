{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "startup-pre-admission",
  "description": "Startup-failure window capability; application commands are gated in Rust and plugin IPC is denied.",
  "windows": ["startup-failure"],
  "permissions": []
}
