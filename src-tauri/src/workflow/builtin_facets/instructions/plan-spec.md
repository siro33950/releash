{{project_name}} プロジェクトの実装仕様（アーキテクチャ・技術選定レベルの対応方針）を整理する。

タスク: {{task}}

## 前提

Specファイル（ステップ出力で提供）を読み込み、`## 振る舞い定義` セクションが存在することを確認する。

## 整理すべき項目

- **対応方針**（必須）: 振る舞い定義をどう実現するか
- **対象コンポーネント**（必須）: どのモジュール・ファイルに変更が必要か
- **技術選定**（該当時のみ）
- **検討した代替案**（該当時のみ）
- **トレードオフ**（該当時のみ）
- **リスク**（該当時のみ）
- **影響するテスト**（必須）

## Specファイル更新

Specファイルの `## 実装仕様` セクションを追加または更新する:

```markdown
## 実装仕様

**対応方針**: [振る舞い定義]を実現するために、[対象コンポーネント]に対して[アプローチ]で対応する。

**対象コンポーネント**:
- [サービス名/モジュール名]: [変更内容の概要]

**影響するテスト**:
- [テストスコープと方針]
```

## 出力

Specファイル更新後、specファイルパスを構造化出力として出力する。
