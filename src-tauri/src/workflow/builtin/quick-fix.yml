name: quick-fix
description: 素早いバグ修正ワークフロー
builtin: true

steps:
  - name: fix
    mode: auto
    prompt: fixer

  - name: verify
    mode: auto
    prompt: verifier
    rules:
      - match: NEEDS_FIX
        next: fix
      - match: LGTM
        next: done
    cycle_guard:
      max_iterations: 3

  - name: done
    mode: approval
    prompt: reporter
