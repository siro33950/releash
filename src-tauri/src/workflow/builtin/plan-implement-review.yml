name: plan-implement-review
description: 計画→実装→レビュー→修正ループ
builtin: true

steps:
  - name: plan
    mode: interactive
    prompt: planner

  - name: implement
    mode: auto
    prompt: coder

  - name: review
    mode: auto
    prompt: reviewer
    rules:
      - match: NEEDS_FIX
        next: implement
      - match: LGTM
        next: report
    cycle_guard:
      max_iterations: 5

  - name: report
    mode: approval
    prompt: reporter
