name: nested-fanout-child
description: fanout can reference a composite as its child
nodes:
  main:
    fanout:
      children:
      - inner
  inner:
    fanout:
      children:
      - worker
  worker:
    command: echo work
