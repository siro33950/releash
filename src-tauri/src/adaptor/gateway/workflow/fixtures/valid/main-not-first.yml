name: main-not-first
description: main does not need to be the first key in the nodes map
nodes:
  finish:
    command: printf finish
  main:
    sequence:
      children:
      - start
      - finish
  start:
    command: printf start
