name: fanout-children
description: valid fanout with multiple top-level children
nodes:
  main:
    fanout:
      child:
        - lint
        - test
    rules:
      - next: done
  lint:
    command: cargo clippy
  test:
    command: cargo test
  done:
    command: echo done
