name: routed-switch
description: valid switch routing
schemas:
  verdict:
    type: object
    properties:
      decision:
        type: string
        enum:
          - done
          - retry
    required:
      - decision
    additionalProperties: false
nodes:
  - name: judge
    artifact: verdict
    session:
      permission: edit
      facets:
        instruction: judge
    rules:
      - switch:
          on: decision
          cases:
            done: done
            retry: retry
  - name: done
    session:
      permission: edit
      facets:
        instruction: done
  - name: retry
    session:
      permission: edit
      facets:
        instruction: retry
