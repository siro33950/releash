name: nested-sequence
description: a sequence can reference another sequence as a child
nodes:
  main:
    sequence:
      children:
      - inner
  inner:
    sequence:
      children:
      - worker
  worker:
    command: echo work
