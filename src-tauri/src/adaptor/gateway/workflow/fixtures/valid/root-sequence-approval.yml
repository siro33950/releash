name: root-sequence-approval
description: root sequence can declare completion approval
nodes:
  main:
    sequence:
      children:
      - worker
    completion: approval
  worker:
    command: echo work
