name: legacy-rule-reject
description: reject is not a workflow rule or approval operation
nodes:
  - name: approve
    session:
      permission: ask
      gate: approval
    rules:
      - reject: fix
        next: done
  - name: done
    command: printf done
