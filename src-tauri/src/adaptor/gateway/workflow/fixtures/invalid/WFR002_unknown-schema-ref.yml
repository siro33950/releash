name: unknown-schema-ref
description: input references a missing schema
nodes:
  main:
    input:
      - item: missing-contract
    session:
      provider: claude
      facets:
        instruction: consume
