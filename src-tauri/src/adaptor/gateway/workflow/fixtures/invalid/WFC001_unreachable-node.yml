name: unreachable-node
description: second node is not reachable without a rule
nodes:
  main:
    session:
      provider: claude
      facets:
        instruction: entry
  orphan:
    session:
      provider: claude
      facets:
        instruction: orphan
