name: unreachable-node
description: second node is not reachable without a rule
nodes:
  - name: entry
    session:
      provider: claude
      gate: auto
      facets:
        instruction: entry
  - name: orphan
    session:
      provider: claude
      gate: auto
      facets:
        instruction: orphan
