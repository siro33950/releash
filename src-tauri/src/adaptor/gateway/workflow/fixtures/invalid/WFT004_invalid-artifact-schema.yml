name: invalid-artifact-schema
description: artifact must reference an object schema
schemas:
  verdict:
    type: string
nodes:
  - name: judge
    artifact: verdict
    session:
      provider: claude
      gate: auto
      facets:
        instruction: judge
