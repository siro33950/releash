name: invalid-artifact-schema
description: artifact must reference an object schema
schemas:
  verdict:
    type: string
nodes:
  main:
    artifact: verdict
    session:
      provider: claude
      facets:
        instruction: judge
