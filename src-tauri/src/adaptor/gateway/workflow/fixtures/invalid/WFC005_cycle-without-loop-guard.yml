name: cycle-without-loop-guard
description: cycle must have loop_guard
nodes:
  main:
    session:
      provider: claude
      facets:
        instruction: first
    rules:
      - next: second
  second:
    session:
      provider: claude
      facets:
        instruction: second
    rules:
      - next: main
