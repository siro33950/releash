name: cycle-without-loop-guard
description: cycle must have loop_guard
nodes:
  - name: first
    session:
      provider: claude
      gate: auto
      facets:
        instruction: first
    rules:
      - next: second
  - name: second
    session:
      provider: claude
      gate: auto
      facets:
        instruction: second
    rules:
      - next: first
