name: multiple-next-catch-all
description: only one next catch-all is allowed
nodes:
  main:
    session:
      provider: claude
      facets:
        instruction: route
    rules:
      - next: done
      - next: done
  done:
    session:
      provider: claude
      facets:
        instruction: done
