name: multiple-next-catch-all
description: only one next catch-all is allowed
nodes:
  - name: route
    session:
      provider: claude
      gate: auto
      facets:
        instruction: route
    rules:
      - next: done
      - next: done
  - name: done
    session:
      provider: claude
      gate: auto
      facets:
        instruction: done
