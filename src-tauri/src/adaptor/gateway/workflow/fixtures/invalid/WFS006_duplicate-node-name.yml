name: duplicate-node-name
description: two nodes must not share the same name
nodes:
  main:
    command: cargo build
    rules:
      - next: build
  build:
    command: cargo build
  build:
    command: cargo test
