name: duplicate-node-name
description: two nodes must not share the same name
nodes:
  main:
    sequence:
      children:
      - build
  build:
    command: cargo build
  build:
    command: cargo test
