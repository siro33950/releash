name: legacy-rule-expression
description: routing expression languages are not part of WorkflowDefinition
nodes:
  - name: route
    command: printf done
    rules:
      - expression: result.count > 0
        next: done
  - name: done
    command: printf done
