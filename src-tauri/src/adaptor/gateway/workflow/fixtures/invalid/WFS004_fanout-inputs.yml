name: fanout-inputs
description: fanout node must not declare inputs
nodes:
  main:
    fanout:
      child:
        - lint
        - test
    inputs:
      - request
    rules:
      - next: done
  lint:
    command: cargo clippy
  test:
    command: cargo test
  done:
    command: echo done
