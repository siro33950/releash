name: reserved-artifact-name
description: request is a reserved artifact name and cannot be declared in schemas
schemas:
  request:
    type: object
    properties:
      spec_dir:
        type: string
    required:
    - spec_dir
nodes:
  main:
    session:
      provider: claude
      facets:
        instruction: implement
