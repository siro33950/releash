name: reserved-artifact-name
description: request is reserved
nodes:
  main:
    sequence:
      children:
      - entry:
          rules:
          - next: request
      - request
  entry:
    session:
      provider: claude
      facets:
        instruction: entry
  request:
    session:
      provider: claude
      facets:
        instruction: request
