name: reserved-artifact-name
description: request is reserved
nodes:
  main:
    session:
      provider: claude
      facets:
        instruction: entry
    rules:
      - next: request
  request:
    session:
      provider: claude
      facets:
        instruction: request
