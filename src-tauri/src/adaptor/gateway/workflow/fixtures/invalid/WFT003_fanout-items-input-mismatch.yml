name: fanout-items-input-mismatch
description: literal fanout items must match child input
schemas:
  target:
    type: integer
nodes:
  main:
    fanout:
      child: worker
      items:
        - not-an-integer
  worker:
    input:
      - item: target
    command: echo {{ item }}
