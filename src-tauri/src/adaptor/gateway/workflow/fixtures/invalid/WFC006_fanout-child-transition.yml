name: fanout-child-transition
description: fanout child cannot be a normal transition target
nodes:
  main:
    sequence:
      children:
      - dispatch
      - source:
          rules:
          - next: worker
  dispatch:
    fanout:
      children:
      - worker
  worker:
    command: echo work
  source:
    command: echo source
