name: fanout-child-transition
description: fanout child cannot be a normal transition target
nodes:
  - name: fanout
    fanout:
      child: worker
    rules:
      - next: source
  - name: worker
    command: echo work
  - name: source
    command: echo source
    rules:
      - next: worker
