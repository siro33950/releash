name: fanout-child-transition
description: fanout child cannot be a normal transition target
nodes:
  main:
    fanout:
      child: worker
    rules:
      - next: source
  worker:
    command: echo work
  source:
    command: echo source
    rules:
      - next: worker
