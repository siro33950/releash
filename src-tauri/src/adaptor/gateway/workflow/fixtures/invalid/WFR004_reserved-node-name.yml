name: reserved-node-name
description: children is a reserved node name
nodes:
  main:
    sequence:
      children:
      - entry:
          rules:
          - next: children
      - children
  entry:
    session:
      provider: claude
      facets:
        instruction: entry
  children:
    session:
      provider: claude
      facets:
        instruction: work
