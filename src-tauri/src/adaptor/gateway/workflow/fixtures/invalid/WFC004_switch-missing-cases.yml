name: switch-missing-cases
description: non-exhaustive switch requires next
schemas:
  verdict:
    type: object
    properties:
      decision:
        type: string
        enum:
          - done
          - fix
    required:
      - decision
nodes:
  - name: judge
    artifact: verdict
    session:
      provider: claude
      gate: auto
      facets:
        instruction: judge
    rules:
      - switch:
          on: decision
          cases:
            done: done
  - name: done
    session:
      provider: claude
      gate: auto
      facets:
        instruction: done
