name: unreachable-subgraph
description: unreachable subgraph edges do not make targets reachable
nodes:
  - name: entry
    session:
      permission: edit
      facets:
        instruction: entry
  - name: orphan
    session:
      permission: edit
      facets:
        instruction: orphan
    rules:
      - next: target
  - name: target
    session:
      permission: edit
      facets:
        instruction: target
