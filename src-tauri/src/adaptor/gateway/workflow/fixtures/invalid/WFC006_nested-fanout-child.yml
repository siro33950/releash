name: nested-fanout-child
description: fanout cannot reference another fanout as its child
nodes:
  main:
    fanout:
      child: inner
  inner:
    fanout:
      child: worker
  worker:
    command: echo work
